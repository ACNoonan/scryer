//! `scryer-fetch-solana` — Solana RPC fetchers.
//!
//! Vault-delta swap extraction (Raydium v4, then v0.2 CLMM/DLMM),
//! account-state pulls, signature scans. Speaks JSON-RPC only via the
//! local `scryer-proxy` daemon — provider keys, retry, hedging, and
//! quota detection are not this crate's concern.
//!
//! Stub crate; v0.1 implementation lands in a later phase.
