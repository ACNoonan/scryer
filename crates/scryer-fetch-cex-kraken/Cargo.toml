[package]
name = "scryer-fetch-cex-kraken"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Kraken public REST clients — trades, OHLC, funding. Owns its own retry/rate-limit logic; no proxy."

[lib]
path = "src/lib.rs"
