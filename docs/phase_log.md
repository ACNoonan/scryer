# scryer - Phase Log

Compact operational ledger. Detailed rationale lives in `methodology_log.md`; schema contracts live in `docs/schemas.md`; forward work lives in `wishlist.md`; v0.2 plan lives in `docs/platform_plan.md`.

Last compacted: 2026-05-02.

## Current Status

- v0.1 shipped broad fetcher/schema coverage plus launchd operations.
- v0.2 platform initiative is active: schema namespace taxonomy is phase 83; workflow-runner design is phase 84.
- Done means code shipped plus at least one canonical parquet partition for the declared range. Code-only rows stay data-pending.
- `docs/schemas.md` is the canonical field-level schema reference.

## Done Index

| Item | Schema / Artifact | Phase | Operational Note |
|---|---|---|---|
| 1 | `kamino_liquidation.v1` | 17 | Klend liquidation event panel |
| 2 | `jupiter_lend_liquidation.v1` | 18 | Fluid/Jupiter Lend liquidation panel |
| 3 | `fluid_vault_config.v1` | 19 | Fluid xStock vault config snapshot |
| 4 | `kamino_reserve.v1` | 28b | Kamino reserve config snapshot |
| 5 | `kamino_obligation.v1`, `kamino_obligation_position.v1` | 31 | Klend borrower-book snapshot |
| 6 | `loopscale_loan.v1`, `loopscale_loan_collateral.v1` | 32 | Loopscale credit-book snapshot |
| 7 | `jito_bundles.v1` | 29 | Jito bundle metadata enrichment |
| 8 | `oracle_context.v1` | 30 | Pre/post oracle observations |
| 9-13 | `v5_tape.v1`, `kamino_scope.v1`, `pyth.v1`, `redstone.v1`, `kraken_funding.v1` | 14-21, 26 | soothsayer daemon imports/migrations |
| 14 | `yahoo.v1::Bar`, `earnings.v1` | 33 | Stooq/Finnhub equity bars + earnings |
| 15 | `backed.v1`, `nasdaq_halts.v1` | 34 | RSS corp-actions + Nasdaq halts |
| 15a | `yahoo_corp_actions.v1` | 61 | Yahoo dividends/splits panel |
| 15b | `nasdaq_halts.v1` historical | 62 | Wayback historical halts; partial coverage disclosed |
| 16 | `fred_macro.v1` | 35 | FRED macro calendar |
| 17 | `chainlink_data_streams.v1` | 60 | continuous Chainlink report tape |
| 18 | `xstock_holders.v1` | 50 | top-N holders and owner-program decomposition |
| 20 | `evm_liquidation.v1` | 52 | Aave V3 + Spark liquidation panel |
| 23 | `pyth_publisher.v1` | 37 | Pythnet per-publisher tape |
| 25 | `cme_intraday_1m.v1` | 38, 39, 63 | CME schema/fetcher + 2018-2026 Databento backfill |
| 26 | `drift_liquidation.v1` | 40 | Drift liquidations |
| 27 | `jito_tip_floor.v1`, `solana_priority_fees.v1` | 42, 43 | Jito tip floor + priority fees |
| 28 | `mango_v4_liquidation.v1`, `mango_v4_oracle_config.v1` | 44 | Mango v4 liquidation + oracle config |
| 29 | `cex_perp_funding_multi.v1` | 41 | multi-venue perp funding |
| 30, 33 | `cboe_indices.v1` | 47 | VIX-family + SKEW historical bars |
| 31 | `deribit_iv.v1` | 46 | Deribit DVOL tape |
| 34 | `edgar_8k.v1` | 51 | SEC 8-K filing index |
| 35 | `fred_macro_extended.v1` | 45 | FRED daily macro series |
| 40 | `raydium_pool_metadata.v1` | 48 | Raydium v3 API pool metadata |
| 41 | `geckoterminal_ohlcv.v1` | 49 | GeckoTerminal historical OHLCV |
| 44 | `pyth_poster_post.v1`, `pyth_poster_tx.v1` | 52-54, 64, 65 | poster mirror/detail tapes and staged submitter |
| 45 | CEX stock-perp venues | 55-58 | venue connectors and Kraken historical path shipped |
| 46 | `marginfi_reserve.v1` | 69 | MarginFi Banks snapshot; no direct xStock Banks found |
| 48 | `chainlink_data_streams.v1` v11 delta | 67 | v11 nullable price fields appended |
| 49 | Chainlink cadence/plist | 66 | cadence audit and forward Chainlink launchd job |
| 50 | freshness/deploy/proxy recovery | 70, 72, 73 | loudly-failing pipeline slices A/C/D shipped |
| 51 | Paper-4 Phase-A xStock AMM panel (`jito_bundle_tape.v1`, `validator_client.v1`, `clmm_pool_state.v1`, `dlmm_pool_state.v1`, `dex_xstock_swaps.v1` forward-poll) | 97, 98, 101, 103, 107 | Slot-resolution xStock AMM capture; DLMM data-shipped via runner-tick. |
| v0.1 scope-2 | `trade.v1` Kraken live fetcher | 74 | canonical Kraken spot trades fetcher |

## Code-Shipped, Data-Pending

| Item | Schema / Artifact | Code Phase | Promotion Action |
|---|---|---|---|
| 24 | `dex_xstock_swaps.v1` | 36 | Persist canonical multi-venue panel. |
| 37 | `backed_nav_strikes.v1` | 59 | Persist canonical partitions; one-shot smoke is not enough. |
| 45 | `cex_stock_perp_tape.v1` | 55, 57 | Complete canonical multi-month/backfill tape. |
| 45 companion | `cex_stock_perp_ohlcv.v1` | 56, 57 | Complete canonical OHLCV backfill; Phemex OHLCV separately blocked. |
| 49a | `pyth.v1` Benchmarks backfill | 71 | Run >=90d operator backfill and verify canonical partitions. |
| LVR Job 2 | `swap.v1` via Helius enhanced API | 79 | Run 26h spot-check against vault-delta archive before 180d run. |
| LVR Job 2 | `swap.v1` via Flipside import | 77 | Superseded by phase 79; keep only as access-path dead end. |
| 52 | `volatility.yahoo.single_stock_iv.v2` | 106, 107 | First runner canonical rows landed phase 107. Promotion to fully shipped still gated on ~20 weekends of accumulation OR a paid-venue backfill (OptionMetrics/CBOE) at `volatility.<paid>.single_stock_iv.v2`. |
| 54 | `oracle.soothsayer_v6.band_tape.v2` | 110 | **Paused 2026-05-03 — methodology-dependent.** Soothsayer-side methodology under revisit; publisher daemon may be redone. Manifest renamed to `ops/sources/soothsayer-band-tape.toml.paused` and removed from staged dir so `runner-tick` no longer fires it. Schema/fetcher/CLI/methodology row remain in tree; if the post-A4 wire format is preserved by the new methodology, resume is a rename + `scryer deploy`. If it changes, resume is a v3 (or new venue) bump per Hard Rule 2. See wishlist Item 54 (Blocked / Gated section) for the gating dependency. |

## Blocked / Retracted

| Item | Status | Operational Note |
|---|---|---|
| 21 | retracted | Chainlink Streams tape premise replaced by Data Streams verifier tape. |
| 22 | retracted | Switchboard On-Demand tape not a near-term comparator source. |
| 39 | retracted | Removed from active work; do not re-propose without new evidence. |
| 45 Phemex OHLCV | blocked | Public kline endpoint is US-IP geo-blocked; ticker endpoint works. |
| 36, 38 | gated | Multi-class scope extensions wait on scope decision. |

## Recent / Load-Bearing Phases

| Phase | Date | Artifact | Why It Matters Operationally |
|---|---|---|---|
| 66 | 2026-04-29 | Chainlink cadence/plist | Established 24/7 oracle forward cadence. |
| 67 | 2026-04-29 | Chainlink v11 decode | v11 reports now decode into v1 nullable columns. |
| 68 | 2026-04-30 | Priority-0 deep scan | Produced operational event-count findings for original trilogy. |
| 69 | 2026-05-01 | `marginfi_reserve.v1` | MarginFi reserve snapshot shipped; direct xStock Banks absent. |
| 70 | 2026-04-30 | `scry freshness` | Detects silent stopped tapes. |
| 71 | 2026-05-01 | Pyth Benchmarks backfill | Code shipped; long operator run pending. |
| 72 | 2026-05-01 | proxy quarantine self-clear | Admin endpoint + recovery probes replace daemon restart. |
| 73 | 2026-05-01 | `scryer deploy` | One-command build/sync/reload workflow. |
| 74 | 2026-05-01 | Kraken spot trades | Live canonical Kraken spot-trade fetcher. |
| 77 | 2026-05-01 | Flipside LVR path | Superseded dead-end; sales-call access wall. |
| 79 | 2026-05-01 | Helius enhanced LVR path | Current LVR Job 2 path; spot-check required. |
| 80 | 2026-05-01 | Paper-4 Phase-A specs | Slot-resolution xStock AMM schemas/spec locked. |
| 81 | 2026-05-01 | `jito_bundle_tape.v1` amendment | Jito bundle source amended to on-chain heuristic capture. |
| 83 | 2026-05-01 | schema namespace taxonomy | v2 naming and migration plan locked. |
| 84 | 2026-05-01 | workflow runner design | manifest/sensor/parquet-checkpointed runner locked. |
| 85 | 2026-05-02 | `SchemaId` type | Closed-domain enum, parser, and uniqueness gate in `scryer-schema`. |
| 86 | 2026-05-02 | source manifest format | Locked TOML schema; `ops/sources/kraken-trades.toml` worked example. |
| 87 | 2026-05-02 | manifest parser/validator | New `scryer-manifest` crate parses + validates `ops/sources/<id>.toml` against the manifest lock and `KNOWN_V1_SCHEMAS`/`SchemaId` registries. |
| 88 | 2026-05-02 | `internal.scryer.workflow_run.v2` | First v2-namespace schema; runner attempt checkpoint with closed `status`/`publish_status` vocabularies. Code-shipped, data-pending until M3.3 runner emits rows. |
| 89 | 2026-05-02 | sensor primitives | New `scryer-sensors` crate evaluates parsed `Sensor` against `(now, prev_fire_at, DatasetState)` and returns structured `Decision`/`FireReason`/`HoldReason`. Pure function; runner composes with manifest-level gates. |
| 90 | 2026-05-02 | runner binary | New `scryer-runner` crate + `bin/scryer-runner` binary (`tick`/`check`/`once`/`dry-run`). Composes manifest parser + sensor evaluator + `internal.scryer.workflow_run.v2` checkpoint + `scryer-store` writer; persistent JSON state file. v0 launchd-driven (single-shot tick). |
| 91 | 2026-05-02 | M3.4 soak prep | Code-shipped, data-pending. `kraken-trades.toml` source label flipped to `kraken:Trades:runner` for soak attribution; `com.adamnoonan.scryer.runner-tick.plist` lands; integration tests exercise `RealCommandRunner` + `ParquetWorkflowRunSink` round-trip; `docs/m3_4_soak_protocol.md` documents pass/fail criteria for the operator-driven 24h+24h soak. |
| 92 | 2026-05-02 | M3.4 closed | Done — code + canonical data. First live runner fire wrote 13,381 SOLUSD trades to `kraken/trades/v1/pair=SOLUSD/year=2026/month=05/day=0[12].parquet` and one `internal.scryer.workflow_run.v2` row (succeeded/published, 16s). `com.adamnoonan.scryer.runner-tick` plist loaded; ticks autonomously, sensor HOLDs within interval. |
| 93 | 2026-05-02 | M3.5 Phase A | Two manifests added (`geckoterminal-trades.toml`, `redstone-tape.toml`) and staged. Runner-tick auto-discovered both on its next launchd-spawned tick and fired each (`FirstRun` reason, succeeded/published). Legacy `:launchd`-attributed plists remain loaded for parallel-soak; operator retires them after the soak window. Phase B (60s-cadence + proxy-routed plists) deferred to M3.6. |
| 94 | 2026-05-02 | M3.6 prerequisites + first Phase-B manifest | `Engine::tick` gained `TickFilter` (`--only` / `--skip`); state refactored to per-manifest files at `<dataset>/.scryer-runner-state/<id>.json` to eliminate the lost-update race that caused redstone-tape to fire every 60s instead of 600s; legacy single-file state migrates on first load. First Phase-B migration: `pyth-tape` under dedicated `com.adamnoonan.scryer.runner-pyth-tape.plist` (`tick --only pyth-tape`, 60s) with multi-manifest plist gaining `--skip pyth-tape`. 8 consecutive succeeded pyth-tape fires verified live. |
| 95 | 2026-05-02 | M3.7 first analytics workflow | New `internal.scryer.workflow_run_summary.v2` schema + `scry analytics workflow-runs` subcommand + `analytics-workflow-runs.toml` manifest with `daily(00:30Z)` sensor. First derived v2 schema; first daily-sensor production fire. 5 canonical summary rows landed covering 57 runner fires across all 5 active manifests. |
| 96 | 2026-05-02 | MX.2 freshness SLA + MX.3 dead-letter | New `internal.scryer.freshness_check.v2` (per-manifest staleness, severity ∈ ok/stale/missing/failing) + `internal.scryer.dead_letter.v2` (failed-run capture with `step_args_json` for replay). New analytics subcommands `scry analytics freshness-check` (every 5min) + `scry analytics dead-letter-extract` (hourly). Live: 8 freshness_check rows (all ok), 6 dead_letter rows capturing real proxy/transport failures with replay context. |
| 97 | 2026-05-02 | 51a `jito_bundle_tape.v1` data-shipped | Wishlist 51a Done. `ops/sources/jito-bundle-tape.toml` (interval 60s, `--latest-slots 200`, `--inter-slot-delay-ms 100`) on dedicated Phase-B plist `com.adamnoonan.scryer.runner-jito-bundle-tape`; multi-manifest `runner-tick` plist gains `--skip jito-bundle-tape`. First runner fires walked 1,391 slots and wrote 44,737 rows under `_source=jito:bundle-tape:runner`; subsequent fires settle ~150s wall-clock per 200-slot window (within partial-coverage caveat). Operational note: an unpaced first fire briefly tripped all proxy upstream providers into quarantine for ~1hr; 100ms inter-slot pacing prevents recurrence. Legacy DRAFT `com.adamnoonan.scryer.jito-bundle-tape.plist` (referenced nonexistent `watch` subcommand) retired. |
| 98 | 2026-05-02 | 51b `validator_client.v1` data-shipped | Wishlist 51b Done — code + canonical data. New `scryer-fetch-solana::validator_client` joins `getEpochInfo` + `getClusterNodes` + Stakewiz `/validators` + Jito kobe `/api/v1/validators` to label each (epoch, leader_pubkey) as `bam`/`jito-agave`/`frankendancer`/`agave-vanilla`/`unknown`. Classification: version starts with `0.` → `frankendancer`; Kobe `running_bam=true` → `bam`; Kobe `running_jito=true` OR Stakewiz `is_jito=true` → `jito-agave`; Stakewiz `is_jito=false` → `agave-vanilla`; cross-source version disagreement → `unknown`. New `scry solana validator-client` flat CLI; `ops/sources/validator-client.toml` manifest (`interval(3600s)`, runs on multi-manifest `runner-tick`); new `venue::SOLANA_VALIDATOR` constant. First fire (post-Kobe integration) wrote 1,954 rows for epoch 965: **338 bam / 305 jito-agave / 87 frankendancer / 23 agave-vanilla / 1,201 unknown (61.5%)**. The 53% jito→bam reclassification reveals Kobe-confirmed BAM has significant adoption among jito-staked validators. Unknown rate is dominated by Stakewiz validators absent from the proxy provider's gossip view (1,193 of 1,201); per the schema lock the unknown-rate is itself a Phase-A diagnostic. Legacy DRAFT `com.adamnoonan.scryer.validator-client-refresh.plist` (referenced nonexistent `refresh` subcommand) retired. |
| 99 | 2026-05-02 | PR.1 source criticality tiers | New optional `[criticality]` block on every manifest, with closed `tier` enum (`tier-0`..`tier-3`) + freeform `owner` and `consumer_impact`. Methodology lock extended (Source Manifest Format) + new dated section. 7 in-tree manifests tagged: 2× tier-0 (oracle foundations: redstone, pyth), 3× tier-1 (kraken-trades, geckoterminal-trades, analytics-freshness-check), 2× tier-2 (analytics-workflow-runs, analytics-dead-letter). Sets up tier-aware behavior for PR.5 (alert routing) and PR.8 (SLO severity). |
| 99b | 2026-05-02 | `dex_xstock_swaps.v1` doc-namespace fix | Schema doc storage line corrected to `dataset/dex_xstock/swaps/v1/symbol={X}/...` to match shipped reality (`venue::DEX_XSTOCK`, partition by symbol). The earlier `solana_dex/xstock_swaps/v1` path was never used in code or shipped data. Doc-only; no code or data movement. (Renumber from 99 → 99b after collision with PR.1 source-criticality-tiers row.) |
| 100 | 2026-05-02 | 51b BAM labeller integrated | Jito kobe `/api/v1/validators` joined as a third source on `validator_client.v1`. New `KobeValidator` decode + `running_bam`/`running_jito` cross-check with Stakewiz `is_jito`. Classification now distinguishes BAM from plain jito-agave: 338 BAM / 305 jito-agave on epoch 965 (was 0 / 642 pre-Kobe). |
| 101 | 2026-05-02 | 51e `dex_xstock_swaps.v1` forward poll | Wishlist 51e Done — code + canonical data. Added `--once` / `--lookback-secs` flags to `scry solana dex-xstock-swaps` (mutually exclusive with `--start`/`--end`). New `ops/sources/dex-xstock-swaps.toml` manifest (`interval(60s)`, 120s lookback, `--use-get-transaction` for proxy-routed stage 2). New `com.adamnoonan.scryer.runner-dex-xstock-swaps.plist` Phase-B per-manifest plist; multi-manifest `runner-tick.plist` `--skip dex-xstock-swaps`. First runner fire wrote 1 swap row under `_source=dex_xstock:swaps:runner` (4s wall-clock). Range backfill of `[2025-07-14, forward-cursor)` is a separate operator job, deliberately not encoded in the manifest. Legacy DRAFT `com.adamnoonan.scryer.dex-xstock-swaps.plist` retired previously (phase 99). |
| 102 | 2026-05-02 | `scryer deploy` cohesion | `scryer deploy` now builds + copies `scryer-runner` alongside `scry`/`scryer-proxy`; rsyncs the full `ops/sources/` tree (including `data/` subdir for manifest-referenced static files) to staged `manifests/`; uses `launchctl kickstart -k` instead of bootout/bootstrap (eliminates the bootstrap-during-shutdown race that downed proxy + portal during the 51b session); guards against DRAFT plists at top-level (`grep 'DRAFT — pending'` in first 30 lines, prints `[skip]`); strips `com.apple.{provenance,quarantine}` xattrs and warms up each binary with `--version` to head off Gatekeeper SIGKILL on first launchd-spawned tick. `ops/launchd/retired/README.md` rewritten to explicitly catalogue both "superseded" and "DRAFT" use cases. |
| 103 | 2026-05-02 | 51c `clmm_pool_state.v1` data-shipped | Wishlist 51c Done — code + canonical data. New `scryer-fetch-solana::clmm_pool_state` decodes both Whirlpool and Raydium-CLMM `PoolState` accounts from `getMultipleAccounts(base64)` + `getBlockTime(slot)`; layouts hand-coded from the Anchor account structs (Whirlpool: 261-byte minimum; Raydium CLMM: 325-byte minimum, `fee_protocol` nullable since Raydium keeps it in `amm_config`). New `pool_discovery` module hits GeckoTerminal `/tokens/{mint}/pools` for live pool enumeration; static `--pools-file` mode is the load-bearing path because GT free tier rate-limits the 8-mint sweep. New `scry solana clmm-pool-state` flat CLI; new `venue::SOLANA_DEX` constant; new `ops/sources/clmm-pool-state.toml` manifest (`interval(60s)`, runs on multi-manifest `runner-tick`, ~500ms wall-clock per fire); curated 40-pool list checked into `ops/sources/data/clmm-pools.txt` (28 raydium-clmm + 12 orca-whirlpools across 5 of 8 xStock mints; remaining 3 mints can be filled in on a regenerate when GT cools down). First runner fire wrote 80 rows (40 unique pools × 2 slots) under `_source=rpc:getMultipleAccounts:clmm-pool-state:runner`. Decoder-side `_dedup_key=clmm_pool_state:{pool}:{slot}` makes overlapping fires free. Legacy DRAFT `com.adamnoonan.scryer.clmm-pool-state-watch.plist` retired previously. |
| 104 | 2026-05-02 | 51d deferred | `dlmm_pool_state.v1` schema + DRAFT plist remain code-pending. Deferred from this session because the Meteora DLMM `LbPair` account doesn't carry the active-bin reserve directly — the `reserve_x`/`reserve_y` fields require a second `getMultipleAccounts` against the corresponding `BinArray` PDA (derived from `(lb_pair, active_id / 70)` seeds), and the schema makes those fields non-nullable. Implementing the BinArray PDA derivation + decode is a careful next-session task; partial-correctness shortcuts (e.g. zeroing reserves) would emit silently-wrong rows under the active dedup key. Wishlist 51d entry unchanged. |
| 106 | 2026-05-02 | 52 `volatility.yahoo.single_stock_iv.v2` code-shipped | Wishlist 52 code-shipped, data-pending. First v2 schema in the `volatility` domain. Methodology lock `Single-Stock IV Schema - 2026-05-02` (per-venue split, ATM = nearest strike at front-week expiry > capture+7d, daily capture / Friday consumption). New `crates/scryer-fetch-equity-options` crate with `yahoo` module: cookie + crumb auth dance against `query2.finance.yahoo.com/v1/test/getcrumb`, then `?crumb=...` on `/v7/finance/options/{symbol}` calls. Two-call shape per symbol (front chain → expiration list → targeted chain if needed); ATM IV is the average of call+put `impliedVolatility` at the nearest strike, fallback to whichever side is present. New `scry equity-options iv-snapshot` CLI; `ops/sources/yahoo-single-stock-iv.toml` (`interval(86400s)`, criticality `tier-2`, freshness SLA 172800s). 9 fetcher unit tests + 7 schema round-trip tests pass; `scryer-runner check` validates the new manifest; `scryer-runner once yahoo-single-stock-iv` produced 8 succeeded rows (SPY 13.85% / QQQ 19.80% / AAPL 24.90% / GOOGL 29.72% / NVDA 37.49% / TSLA 42.27% / HOOD 56.73% / MSTR 73.64% at dte=12). Forward-only by source design; paid backfill (OptionMetrics/CBOE/Tradier) lands as separate sibling manifest under same record-type when access is acquired. |
| 107 | 2026-05-03 | 51d `dlmm_pool_state.v1` data-shipped + 52 first runner canonical rows | Wishlist 51d Done — code + canonical data. Operator-side bootstrap (a one-shot `scry solana dlmm-pool-state` GT-discovery fire under `_source=...:bootstrap-verify`, 2026-05-02 23:40Z) seeded `ops/sources/data/dlmm-pools.txt` with 14 Meteora xStock pools across 6 of 8 mints. The runner-tick had been firing the manifest every 60s since but writing zero rows because `ops/sources/data/` is gitignored and the staged copy at `~/Library/Application Support/scryer/manifests/data/dlmm-pools.txt` was the empty placeholder from an earlier deploy — `scry solana dlmm-pool-state --pools-file <empty>` returns `rows_added=0 (no pools matched)` and exits 0, so workflow_run kept showing succeeded/published with `_source=...:runner` rows totally absent. Cutover: `cp ops/sources/data/dlmm-pools.txt ~/Library/Application\ Support/scryer/manifests/data/`. Verification: `scryer-runner once dlmm-pool-state` wrote 14 runner-attributed rows at slot 417356601 / fetched 2026-05-03 17:14:03Z under `_source=rpc:getMultipleAccounts:dlmm-pool-state:runner`. Wishlist item 51 now fully shipped (51a/51b/51c/51d/51e). Same phase: item 52 `volatility.yahoo.single_stock_iv.v2` got its first runner-driven canonical fire at 2026-05-03 17:11:42Z (run_id `01777828302...`, `succeeded`/`published`), 8 rows landed (SPY 13.85% / QQQ 19.80% / AAPL 24.90% / GOOGL 29.72% / NVDA 37.49% / TSLA 42.27% / HOOD 56.73% / MSTR 73.64% at dte=11) under `_source=yahoo:options:v7:runner` against `dataset/volatility.yahoo/single_stock_iv/v2/`. The first runner attempt at 2026-05-02 21:59:49Z had been `failed`/`exit.unknown` (see `analytics-dead-letter` for the captured failure) — the failure cleared on the next interval window. Item 52 stays data-pending per the wishlist's stricter promotion criterion (~20 forward weekends or paid-venue backfill) but the forward-poll is now live. |
| 109 | 2026-05-03 | 53 `nasdaq_halts_intraday.v1` code-shipped (Soothsayer W6 unblock) | Wishlist 53 code-shipped + runner-live, data-pending. New schema `nasdaq_halts_intraday.v1::Bar` (1m OHLCV with `halt_event_id` FK into `nasdaq_halts.v1`, dedup_key `(halt_event_id, ts)`); registered in `KNOWN_V1_SCHEMAS` and `scryer-store::DatasetSchema`. New `scryer-fetch-equities::yahoo_intraday` module — Yahoo `/v8/finance/chart` at `interval=1m`, no auth (browser UA only; the cookie+crumb dance the daily-bars path needed turns out not to gate the chart endpoint at 1m), 7-day rolling backfill horizon (probed live: `period1` older than 7d returns empty `timestamp[]`). New top-level `scry nasdaq halts-intraday` subcommand reads halts-in-window from `nasdaq_halts.v1` yearly partitions, collapses to one Yahoo call per `(symbol, halt_date)` pair, fetches a 2-day window (covers same-day halts AND late-day halts that resume next morning), tags each bar with all halt_event_ids on that day. New `ops/sources/nasdaq-halts-intraday.toml` manifest fires `daily(22:30Z)`, freshness SLA 25h. First runner fire wrote 0 rows because the canonical `nasdaq_halts.v1` table has no halts within Yahoo's 7d window (the AIOS/CISS/DRCT/FOLD/IMUX/OCG halts are all 2026-04-24, 9 days ago — just outside). Symbol scope is "every halted symbol in lookback window", not strictly the Soothsayer 10-symbol universe — those large-caps had zero halts in the past 30d, so a strict scope would yield an empty dataset for months. 6 fetcher unit tests + 4 schema round-trip tests pass; `scryer-runner once nasdaq-halts-intraday` returns `succeeded`. Soothsayer-side W6 disclosure required: pre-2026-04-26 halt windows cannot be backfilled from Yahoo. Paid intraday venue (Polygon/Tradier/Databento US-equity 1m) is the path to deeper history; row schema is shared. |
| 108 | 2026-05-03 | `yahoo.v1` forward-poll cron stand-up (Soothsayer W5 unblock) | New `ops/sources/equities-daily.toml` manifest fires `daily(22:00Z)` over the 10-symbol Soothsayer universe (SPY, QQQ, AAPL, GOOGL, NVDA, TSLA, HOOD, GLD, TLT, MSTR), freshness SLA 25h. Closes the gap discovered during W5 audit: `dataset/yahoo/equities_daily/v1/` had been frozen at `_source=import:legacy:yahoo` since 2026-04-26 — no scryer-side cron had ever fired against `yahoo.v1` (the schema was operator-driven only via ad-hoc `scry equities bars --start ... --end ...`). New `--lookback-days N` flag on `scry equities bars` lets the manifest stay static while pulling a 7-day rolling window for cheap dedup-safe re-pulls. `STOOQ_API_KEY` propagated into the deployed `~/Library/Application Support/scryer/.env` so the runner-spawned subprocess inherits it via dotenvy. First runner fire wrote 4 rows per symbol (Mon Apr 27 → Fri May 1) under `_source=stooq:csv:runner`, _fetched_at 2026-05-03 17:38:39Z. 22:00 UTC was chosen over the brief's 14:30 UTC because Stooq is EOD-only — a 14:30 UTC fire would only ever capture the prior session's bar, leaving Monday's bar absent until Tuesday-evening. Soothsayer's W5 Tuesday-morning forward-coverage cron now reads Monday's `open` reliably. |
| 113 | 2026-05-04 | 47 `marginfi_liquidation.v1` data-shipped — Item 47 promotes to Done | First operator backfill `[2026-05-01, 2026-05-03]` completed cleanly: 6,040 sigs paginated in 12s, stage-2 `getTransaction` proxy-routed in 7m19s, **677 liquidations decoded across 3 daily partitions** under `_source=rpc:getTransaction:marginfi-liquidations:backfill`. Bank-registry parquet auto-load worked: 422/422 banks loaded with 422/422 `insurance_vault_authority` PDAs derived. 100% of the 15 distinct asset_banks and 13 distinct liab_banks observed in the liquidation rows match the registry — the registry-resolution code path is correct end-to-end. Three quality findings surfaced and tracked as Item 47 follow-ons (47.1/47.2/47.3 in `wishlist.md`): (a) `asset_amount_seized` and `insurance_fund_fee_paid` ship at 0 for all 677 rows because the dominant liquidation pattern is flashloan-arb-wrapped — the liquidator's wallet `asset_mint` delta is 0 (asset gets swapped to USDC same-tx via `LendingAccountWithdraw` + Jupiter); the bank's `liquidity_vault` does show the seizure (e.g. -32,207,412 of asset_mint on one sampled tx), so the right v2 primitive is an inner-IX SPL-Transfer walk. (b) `liquidatee_pre_health` / `_post_health` observed range `[-107, 0]` shows the values are weighted USD-deltas, not the [0, 1.0] ratio the schema docstring assumed. (c) `marginfi_reserve.v1` snapshot was captured without an SPL/xStock symbol map → all 422 banks have `asset_symbol="?"` → all 677 liquidations ship `("?", "?")` per_pair. None of these block Item 47 itself: the verification trigger ("≥1 marginfi liquidation tx writes a non-empty partition") fired cleanly. Item 47 promotes to Done; follow-ons sharpen data quality. |
| 112b | 2026-05-04 | 47 — fix off-by-8 in bump offsets; insurance_vault_authority derivation now 100% | The original phase-112 ship indexed `raw_account_b64` at `8 + 145` and `8 + 179` for the two vault-authority bumps, double-counting the 8-byte Anchor disc. The methodology layout block's offsets are absolute (account-start, including the disc) — the table starts with `8 mint: pubkey`, not `0 mint`. Bytes at the wrong offsets were 0 for 265/422 banks (off by 8 lands in `_pad1` on some banks, `collected_insurance_fees_outstanding` low bytes on others — both happen to be 0 frequently). Fixed: `RAW_LIQUIDITY_VAULT_AUTHORITY_BUMP = 145`, `RAW_INSURANCE_VAULT_AUTHORITY_BUMP = 179` (no `+ 8`). Spot check post-fix: all 422 banks have valid Anchor bumps (238 at 255, 90 at 254, 45 at 253, down to 1 at 246; 0 at bump=0). `insurance_vault_authorities_derived = 422 / 422` against the canonical snapshot. Methodology + schema doc updated to remove the "~52%" caveat. |
| 112 | 2026-05-03 | 47 `marginfi_liquidation.v1` — bank-registry parquet auto-load + `insurance_fund_fee_paid` derivation | Two improvements over phase 111 v1: (a) `scryer-store::read_marginfi_bank_registry()` reads the latest snapshot per bank from `marginfi_reserve.v1` parquet (same surface area as `read_kamino_reserve_symbol_map`), surfacing `(bank, asset_mint, asset_symbol, asset_decimals, primary_oracle, insurance_vault_authority_bump, liquidity_vault_authority_bump)`. The two bumps are decoded directly from `raw_account_b64` byte offsets 8+145 and 8+179 (no schema bump needed). (b) `marginfi-liquidations` CLI now defaults to auto-discovering the canonical reserve partition at `<dataset>/<venue>/reserves/v1/`; the `--bank-registry-from-parquet PATH` flag forces a different path; `--bank-registry-json` (existing) takes precedence for hand-curated overrides. The fetcher derives `insurance_vault_authority` PDAs in-process via `Pubkey::create_program_address(&[b"insurance_vault_auth", bank, &[bump]], &MARGINFI_PROGRAM_ID)` using the new solana-sdk path. `extract_liquidations` then walks `tx.account_data` for that authority's positive `asset_mint` delta and emits `insurance_fund_fee_paid` (native u64). Spot-check against the live 422-bank canonical snapshot: 157 banks derive a non-zero-bump PDA cleanly; 265 banks have `iv_auth_bump=0` at the documented offset (likely a Bank-account layout variant — Anchor bumps normally cluster near 255). Bump=0 banks produce meaningless PDAs that never appear in liquidation `account_data`, so `insurance_fund_fee_paid` correctly stays 0 for them — no false positives, just lower coverage until the layout variant is investigated. `liquidator_fee_paid` is now permanently 0 in the schema doc (marginfi-v2 doesn't emit a separate liquidator-fee SPL transfer; the bonus is implicit in the asset/liability ratio). 10 fetcher unit tests (added 2: insurance-fee population + derive_vault_authority guard) + 318 schema/store tests still pass. Methodology + `docs/schemas.md` updated. |
| 111 | 2026-05-03 | 47 `marginfi_liquidation.v1` code-shipped | Wishlist 47 code-shipped, data-pending. New schema `crates/scryer-schema/src/marginfi_liquidation.rs::v1::Liquidation` (33 columns, dedup `marginfi_liquidation:{sig}:{ix_index}`, daily `dataset/marginfi/liquidations/v1/`). Two new shared-infra extensions: (1) `ParsedTx::logs: Vec<String>` populated from `meta.logMessages` on the proxy `getTransaction` path — first scryer fetcher to require logs alongside parsed instructions; (2) Anchor-event-from-logs decode pattern (`Program data: <base64>` → 8-byte disc match → Borsh deserialize), now documented as a methodology section for future Anchor-event consumers. New `crates/scryer-fetch-solana/src/marginfi_liquidations.rs` extractor: walks IXs for MarginFi-v2 program ID (`MFv2hWf31Z9kbCa1snEPYctwafyhdvnV7FZnsebVacA`) + IX disc `[214,169,151,213,251,167,86,219]`, decodes the first `LendingAccountLiquidateEvent` (disc `[166,160,249,154,183,39,23,242]`) from logs, populates 27 of 30 data columns from event + outer-tx + liquidator's asset_mint balance delta. Three native-unit columns (`liquidator_fee_paid`, `insurance_fund_fee_paid`, and to a lesser degree `asset_amount_seized` for non-trivial flows) ship reserved at 0 pending follow-on capture of bank `liquidity_vault_authority` / `insurance_vault_authority` PDAs in `marginfi_reserve.v1` — methodology + schemas.md amended to reflect the v1 deferral. New `scry solana marginfi-liquidations --start --end [--bank-registry-json]` CLI; uses `getTransaction` exclusively (no Helius parseTransactions fallback because parseTransactions doesn't surface raw logs). 8 fetcher unit tests + 4 schema round-trip tests pass; full workspace `cargo test --lib` clean. First live tx pending: no marginfi liquidations observed in canonical scryer data yet. Phase 110b methodology amendment row already shipped (renumber to 110b in this same day's batch). |
| 110b | 2026-05-03 | Item 47 `marginfi_liquidation.v1` methodology amendment | Pre-flight pass against `idl/marginfi/marginfi-v2.json` showed the locked schema specified 6 oracle-price f64s and 3 native-unit u64s the Anchor `LendingAccountLiquidateEvent` does not carry. Methodology + `docs/schemas.md` amended 2026-05-03: oracle prices removed from the row (carried as `asset_oracle` / `liab_oracle` pubkey join keys instead, joining via `oracle_context.v1` per `kamino_liquidation.v1` precedent); `asset_amount_seized` / `liquidator_fee_paid` / `insurance_fund_fee_paid` retained, sourced from inner SPL Token Transfer decode. Dedup key gains explicit `ix_index` u32 column. IDL discs pinned: IX `[214,169,151,213,251,167,86,219]`, event `[166,160,249,154,183,39,23,242]`. Code phase pending. |
| 110 | 2026-05-03 | 54 `oracle.soothsayer_v6.band_tape.v2` code-shipped | Wishlist 54 code-shipped, data-pending. First v2 schema in the `oracle` domain. Methodology lock `Soothsayer Lending-track Band Tape - 2026-05-03`. New `crates/scryer-schema/src/oracle_soothsayer_v6_band_tape.rs::v2::Row` (one row per observed publish; partition key `profile`, dedup `(symbol, publish_slot)`, `symbol_class` enrichment hardcoded to mirror soothsayer M6_REFACTOR.md A1). New `scryer-fetch-solana::soothsayer_band_tape` poll module: 1× `getMultipleAccounts` (10 PDAs) + 1× `getBlockTime`, decode delegated to `soothsayer-consumer` path-dep at `../soothsayer/crates/soothsayer-consumer` (same crate that the on-chain Anchor program uses for its byte-offset layout — single source of truth). PDAs derive at startup from `--symbols × --program-id` (default devnet `AgXLLTmUJEVh9EsJnznU1yJaUeE9Ufe7ZotupmDqa7f6`); SPY-PDA derivation verified by unit test against the live devnet account `HfMaU9Qa54fp1V3uh11Qec81RgKUgzT6mxvFkmZ6V3LH`. Pre-A4 `profile_code=0` rows filtered at the fetcher boundary; default `--profile-codes 1,2` accepts both Lending and AMM. New `venue::ORACLE_SOOTHSAYER_V6 = "oracle.soothsayer_v6"` and `KNOWN_V2_SCHEMAS` entry. New `scry solana soothsayer-band-tape` CLI; new `ops/sources/soothsayer-band-tape.toml` manifest (`interval(60s)`, freshness SLA 86700s = 1d + slack, criticality `tier-2`, runs on multi-manifest `runner-tick`). 5 schema round-trip tests + 3 fetcher PDA-derivation tests pass. First canonical row gated on the soothsayer-side publisher daemon (M6_REFACTOR Phase A5 step 2); until then runner fires emit zero rows and exit 0 (PDA accounts don't exist yet — expected pre-publisher-go-live). AMM-track sibling Phase B reuses the same fetcher unchanged. |
| 105 | 2026-05-02 | 51d `dlmm_pool_state.v1` code-shipped | Wishlist 51d code shipped, data-pending. New `scryer-fetch-solana::dlmm_pool_state` runs the two-pass fetch the schema requires: (1) `getMultipleAccounts(pools)` decodes each pool's Meteora `LbPair` (Anchor account, IDL `lb_clmm` v0.10.1; offsets hand-coded from the IDL — `active_id` at byte 76, `bin_step` at 80, `parameters.protocol_share` at 32, `v_parameters.volatility_accumulator` at 40); (2) derives the active bin's `BinArray` PDA from `[b"bin_array", lb_pair, bin_array_index_le_bytes]` (signed `i64`, floor-div on `active_id` via `div_euclid`); (3) `getMultipleAccounts(bin_arrays)` reads `amount_x`/`amount_y` from the active bin slot (BinArray is fixed 10136 bytes; bin records 144 bytes each at offset `56 + 144*local`). Per-row `slot` is the LbPair-batch slot, not the BinArray batch's. `decode_active_bin_reserves` validates the BinArray's `lb_pair` tag against the requested pool before trusting the reserves (defends against bogus PDA collisions / mid-migration races). New `scry solana dlmm-pool-state` flat CLI mirrors `clmm-pool-state` (GT discovery filtered to `dex.id == "meteora"` or `--pools-file`). New `ops/sources/dlmm-pool-state.toml` manifest (`interval(60s)`, runs on multi-manifest `runner-tick`, budget 10 RPC/run, criticality `tier-1`). New `solana-sdk` dep on `scryer-fetch-solana` for `Pubkey::find_program_address`. 10 unit + 3 fixture tests pass against verbatim `MeteoraAg/dlmm-sdk` `commons/tests/fixtures` lb_pair + bin_array dumps (vendored at `crates/scryer-fetch-solana/tests/fixtures/meteora_dlmm/`). Pool list `ops/sources/data/dlmm-pools.txt` is intentionally empty at first ship — operator runs `scry solana dlmm-pool-state` (no `--pools-file`) to populate from live GT discovery; the manifest's 300s freshness SLA is the forcing function. |
| 112 | 2026-05-03 | PR.2 runner retry policy code-shipped | Methodology lock `Runner Retry Policy - 2026-05-03`. New optional `[retry]` block on every manifest with closed `retry_on` vocabulary `{transient, timeout, nonzero_exit}`; default is no retry so undeclared manifests behave exactly as pre-PR.2. New `scryer_manifest::Retry` / `RetryFamily` / `RetryOnSet` types with full validation (max_attempts ≥ 1 and ≤ 10 hard cap, backoff_max ≥ initial, jitter_ratio ∈ [0,1], unknown families rejected). `CommandRunner` trait extended with default `run_with_timeout(cmd, args, timeout)` method; `RealCommandRunner` overrides with a piped-`Child` + reader-thread + `try_wait` poll loop that emits `status="timed_out"` / `error_class="timeout"` on deadline expiry. `Engine::fire` rewritten as a per-manifest retry loop: persists one `internal.scryer.workflow_run.v2` row per attempt (carrying `attempt` + `retry_of_run_id` chain), records `last_fire = triggered_at_unix_secs` BEFORE the loop so concurrent ticks suppress against the trigger time, and decides retry via the locked `error_class → RetryFamily` mapping. Backoff is `min(cap, initial * 2^(attempt-1)) * (1 ± jitter_ratio)` with jitter derived from `unix_nanos_now()` (no `rand` dep). 12 new manifest tests + 9 new runner tests + 2 real-process timeout tests (`/bin/sleep` killed at 500ms). New `every_in_tree_source_manifest_parses` test loads every `ops/sources/*.toml` so future regressions in any manifest fail fast. No PR.* knobs added to in-tree manifests yet — operators opt in per manifest as the first failure modes appear. |
| 113 | 2026-05-03 | M3.6 Phase B remainder code-shipped | All five remaining Phase-B plists migrated to runner manifests. Four new `ops/sources/<id>.toml` manifests (`backed-nav-strikes`, `cex-stock-perp-tape`, `kamino-scope-tape`, `v5-tape`), each tier-1, 60s `interval`, 120s freshness SLA, `--source ...:runner` for soak attribution. Four corresponding per-manifest plists `com.adamnoonan.scryer.runner-<id>.plist` (`tick --only <id>`, 60s, dedicated launchd skip-if-running pacing). Multi-manifest `runner-tick.plist` gains `--skip backed-nav-strikes --skip cex-stock-perp-tape --skip kamino-scope-tape --skip v5-tape`. `chainlink-reports` lands as `ops/sources/chainlink-reports.toml.deferred` mirroring the `.toml.paused` exclusion convention — gated on the same RPC sponsorship decision as the existing `ops/launchd/deferred/com.adamnoonan.scryer.chainlink-reports.plist`; resume = rename to `.toml`, stand up a per-manifest plist, add `--skip chainlink-reports` to `runner-tick`, run `scryer deploy`. `scryer-runner check` validates 19 manifests load (15 prior + 4 new); `scryer-runner dry-run` prints the expected `scry` invocation for each new manifest. Operator action remaining: `scryer deploy` to install the four new plists into `~/Library/LaunchAgents/` and stage the manifests into `~/Library/Application Support/scryer/manifests/`; legacy `:launchd` plists remain loaded for parallel-soak per the M3.4 protocol until the operator retires them. Item M3.6 closes once parallel-soak rows verify in `internal.scryer.workflow_run.v2` and `_source=...:runner` rows land in each canonical partition. |
| 114 | 2026-05-04 | Deploy plist reload, proxy `--version`, portal-shell launchd-only | Ops + desktop shell batch from the M3.6 deploy. (1) `scryer deploy` bootouts + bootstraps any plist whose content changed (`[sync]` bucket), not only `kickstart`, so launchd reloads cached `arguments`/`EnvironmentVariables`; caught when `runner-tick.plist`'s `--skip` list grew fourfold while launchctl still ran stale args from the prior bootstrap. Poll up to ~3s after `bootout` before `bootstrap`; KeepAlive `kickstart -k` skips labels already reload-handled (`set -u`-safe synced-label iteration); verified whitespace-only `[sync]` → `[reload]` and `launchctl print` picks up argv. (2) `scryer-proxy` pre-tokio sniff on `--version` / `-V` / `--help` / `-h` so deploy `--version` probes exit 0 when port 8899 is live instead of falsely warning; no new clap dependency. (3) `scryer-portal-shell` drops embedded `scryer-portal-server` sidecar spawn; probes `http://127.0.0.1:47777/api/health` (ureq) and exits with an `rfd` dialog if unreachable; removes `tauri-plugin-shell`/opener; Tauri codegen output (`gen/schemas/*`) regenerated. |
| 115 | 2026-05-04 | Portal shell bundle + deps (follow 114) | `scryer-portal-shell` removes stale `serde`/`serde_json`/`tokio` deps (webview-only crate). `tauri.conf.json`: `beforeDevCommand`/`beforeBuildCommand` run `npm run dev|build` from the crate cwd (drop `npm --prefix ui`), matching shipped layout; `bundle.targets` narrowed to `["app"]` (adhoc-signed `.app` to `/Applications/`, no DMG). Methodology Decision Index gains `Portal shell distribution` dated row refining the 2026-04-28 Portal invariant (health probe, dialog, bundle shape). |
| 116 | 2026-05-04 | 47.1 marginfi liquidation native-unit fees via inner-IX SPL Transfer walk | Wishlist 47 follow-on 47.1 code-shipped + smoke-validated. The wallet-delta heuristic for `asset_amount_seized` and `insurance_fund_fee_paid` returned 0 for the dominant flashloan-arb pattern (100% of observed liquidations); replaced with a per-IX walk of `lending_account_liquidate.inner_instructions` summing SPL Token Program v1 + Token-2022 `transfer` / `transferChecked` amounts where `source == accounts[7]` (`bank_liquidity_vault`) and `destination == accounts[8]` (`bank_insurance_vault`). New `crates/scryer-fetch-solana/src/types.rs::ParsedIxInfo` carries the jsonParsed `{type, info}` block (with `token_amount_u64()` / `token_checked_amount_u64()` / `source()` / `destination()` accessors); `HeliusInstruction.parsed: Option<ParsedIxInfo>` ships defaulted-None so existing fetchers/tests are unaffected. `get_transactions::convert_ix` reshapes the permissive `serde_json::Value` (some programs emit `"parsed": "<string>"`, e.g. Vote IXs) into `ParsedIxInfo` only when it's an object. `marginfi_liquidations::extract_liquidations` rewires the per-row computation as a per-IX walk; the dead `asset_mint_gain_for_user` heuristic and unused `account_data` import are removed. Smoke against `[2026-05-02, 2026-05-02]` (1744 sigs → 37 liquidations) into a temp dataset writes 37 fresh rows, all with non-zero `asset_amount_seized` and `insurance_fund_fee_paid` (100% population vs. 0% under the prior heuristic; total seized == total insurance, fingerprint of the single-transfer-out-of-liquidity-vault-into-insurance-vault pattern that dominates within the liquidate IX itself). For the spec sample sig `25bhcHB7gL3JR8WnfKBcvcnxs7tcHCjgHeg4puCNe6PVqh1ohfYzpCg3xi5MReGFFKcyJaJM5SF8qo61rNg8uTjp` the new code emits `seized = insurance = 37534` (the insurance-fee fragment); the gross 32,207,412 seizure lives in the same-tx `lending_account_withdraw` IX which is item 47.1.b out-of-scope. Existing 677 canonical rows on `[2026-05-01, 2026-05-03]` retain their zeros (existing-row dedup wins); operator action is to delete and re-fetch those partitions to backfill. 12 fetcher tests (added `populates_native_amounts_from_inner_spl_transfers`, `handles_token_transfer_checked`, `truncated_ix_accounts_dont_panic`, `ignores_inner_transfers_outside_target_vaults`; removed two outdated wallet-delta tests); workspace `cargo test --lib` passes 1009 tests. Methodology + `docs/schemas.md` updated; schema column list unchanged. |
| 118 | 2026-05-04 | 47.3 marginfi_reserve symbol-map re-snapshot | Wishlist 47 follow-on 47.3 done. CLI plumbing (`--symbol-map-json PATH`) added to `scry solana marginfi-reserves`; the existing `mints: &[MintEntry]` registry already exposed by the fetcher was previously hardcoded to the 8-mint `XSTOCK_MINTS`. Optional JSON `Vec<{"symbol","mint"}>` is now merged with the built-in xStock registry (JSON wins on mint collision; xStocks always present so `--xstock-only` semantics are preserved). New `ops/sources/data/marginfi-symbol-map.json` (gitignored alongside `clmm-pools.txt` / `dlmm-pools.txt`) ships 10 mappings: USDC + SOL (canonical from `crates/scryer-fetch-solana/src/types.rs::mints`) plus the 8 xStocks (canonical from `crates/scryer-fetch-dexagg/src/jupiter.rs::XSTOCK_MINTS`). Re-snapshot under `--all --symbol-map-json …` wrote a fresh `dataset/marginfi/reserves/v1/year=2026/month=05/day=04.parquet` (422 rows, 0 deduped, 1 partition). Symbol coverage went from 0/422 non-`?` to **110/422 non-`?` (99 USDC + 11 SOL)**; remaining 312 banks still `?` because their mints aren't in the 10-entry map. As expected, no direct xStock Banks exist in MarginFi-v2 (methodology lock "Live reserve validation found no direct xStock Banks; consumers may need Kamino-position indirection") so the 8 xStock entries in the map are inert against the current bank set but kept for parity with the rest of the codebase. Verification `scry solana marginfi-liquidations --start 2026-12-01 --end 2026-12-01` reloads the registry from the new partition (`banks_loaded=422`, `insurance_vault_authorities_derived=422`). No schema change; 1009 workspace lib tests still pass. Operator action remaining to propagate symbols into the existing 677 liquidation rows on `[2026-05-01, 2026-05-03]` is a delete-and-re-fetch cycle (same carry-forward as 47.1 / 47.1.b); deliberately not done by this phase. Item 47 follow-on 47.3 promotes to Done. |
| 117 | 2026-05-04 | 47.2 marginfi liquidation health-value scale — docstring correction | Wishlist 47 follow-on 47.2 doc-only fix. Phase-113 first-live-tx validation surfaced that the schema docstring on `pre_health` / `post_health` ("sub-1.0 = liquidatable" / "expected ~1.0 after partial liquidation") was wrong: empirical 677-row sample on `[2026-05-01, 2026-05-03]` shows `pre_health ∈ [-107.0630, 0.0000]` and `post_health ∈ [-41.4272, 0.0000]` — values are negative and never approach 1.0. Pinned the actual formula by reading mrgnlabs/marginfi-v2 commit `843aa82d` `programs/marginfi/src/state/marginfi_account.rs::check_pre_liquidation_condition_and_get_account_health` (called from `programs/marginfi/src/instructions/marginfi_account/liquidate.rs:171,439`): `account_health = assets.checked_sub(liabs)` where `(assets, liabs)` come from `get_health_components(.., RiskRequirementType::Maintenance, ..)` — i.e. `asset_value_maint − liability_value_maint`, both `WrappedI80F48` "* In dollars" per the `HealthCache` IDL doc strings. The pre-liquidation gate rejects with `HealthyAccount` when `health > 0`, so **sub-zero = liquidatable**; more-negative = deeper underwater. The post-liquidation invariant asserts both `health <= 0` (else `TooSevereLiquidation`) and `health > pre_liquidation_health` (else `WorseHealthPostLiquidation`), so a successful partial liquidation moves the value strictly upward toward zero but does not cross it — consistent with the observed `[-41, 0]` post range vs. `[-107, 0]` pre range. Three doc surfaces updated to match: `crates/scryer-schema/src/marginfi_liquidation.rs` field docstrings; `docs/schemas.md` `## marginfi_liquidation.v1` row-block inline comments; `methodology_log.md` `MarginFi-v2` entry. No code change, no schema bump (data on disk is correct; this is purely a consumer-facing semantic correction). Workspace `cargo test --workspace --lib` still passes 1009 tests. Item 47 follow-on 47.2 promotes to Done. |
| 122 | 2026-05-04 | CME 1m gap-fill backfill 2026-04-29 → 2026-05-01 (operator one-shot) | One-shot operator backfill at soothsayer agent's request to seat the 2026-05-01 weekend in the M6 forward-tape harness's first report. `scry databento intraday1m --symbols ES=F,NQ=F,GC=F,ZN=F --start 2026-04-29 --end 2026-05-03 --source databento:glbx-mdp3:backfill` wrote 15,802 rows across 12 partitions (4 symbols × 3 trading days: Apr 29, Apr 30, May 1; May 2 is Saturday with no CME session). All symbols now have continuous coverage Apr 26 → May 4 with only the expected Sat 2026-05-02 gap. The new `cme-intraday-1m.toml` `daily(06:00Z)` runner will keep the surface current going forward; this fills the single window between phase 63's prior backfill cursor and today's runner stand-up. No schema or methodology changes; not a wishlist row because it's a deliberate operator-driven gap-fill, not a data-pending verification trigger. |
| 121 | 2026-05-04 | M6 G1.b resolved — CBOE-indices forward-poll manifest (^VIX) + futures resample contract (data-shipped) | Closes the M6 G1.b gap surfaced by phase 120 with a split decision per soothsayer agent: VIX from a new scryer-side CBOE manifest; CME factor futures (ES=F/NQ=F/GC=F/ZN=F) handled soothsayer-side by resampling the already-forward-polled `cme/intraday_1m/v1` 1m bars to daily. New `ops/sources/cboe-indices.toml` (tier-2, freshness SLA 90000s) runs the existing `scry cboe indices` (phase 47, full-history idempotent CSV pull from `cdn.cboe.com`) on `daily(22:30Z)` over the default VIX-family + SKEW set (VIX, VIX9D, VIX1D, VIX3M, VIX6M, SKEW). No CLI changes — the cboe fetcher already pulls full history each call and dedups idempotently on `cboe_indices:{index}:{date}`. First-fire 2026-05-04 21:58Z: all 6 indices populated under `_source=cboe:csv:runner` at `dataset/cboe/indices/v1/index={IDX}/year=2026.parquet`; VIX 2026 partition = 85 rows through Fri 2026-05-01 close (Mon 2026-05-04 settle propagates from CBOE's CSV ~2h post-16:15 ET, so the regular daily(22:30Z) cadence catches it cleanly). ^GVZ and ^MOVE intentionally not in the manifest — CBOE free CSV doesn't serve them; soothsayer panel.py falls back to ^VIX consumer-side per the M6 hand-off brief §5 watch-out #1. Soothsayer-side reads the new path (`dataset/cboe/indices/v1/index=VIX/...`), not the legacy `dataset/yahoo/equities_daily/v1/symbol=^VIX/...` — column shapes differ (cboe_indices.v1 has `index/date/open/high/low/close` vs yahoo.v1's `symbol/ts/open/high/low/close/adj_close/volume`). Wishlist M6 G1.b row promotes to done. |
| 120 | 2026-05-04 | M6 forward-tape readiness — equities-daily expansion + earnings/cme forward-poll manifests (data-shipped) | Three Soothsayer M6 Phase-4 unblockers, code + first-fire-canonical-data shipped same-session. No schema bumps; conforms to existing `Source manifest format - 2026-05-02` and `Workflow runner - 2026-05-01` locks. **G1 (final)**: `ops/sources/equities-daily.toml` `--symbols` extended 10 → 11 (added BTC-USD only; ES=F, NQ=F, GC=F, ZN=F, ^VIX initially attempted but Stooq's free-tier CSV returns zero rows for futures and `No data` for ^VIX — surfaced as wishlist row "M6 G1.b — futures/^VIX daily forward poll" pointing at existing `cme/intraday_1m/v1` and `cboe_indices.v1` for the gap). First-fire 2026-05-04 21:53Z: all 11 symbols' `_fetched_at` advanced. **G2**: new `ops/sources/earnings.toml` runs `scry equities earnings --symbols AAPL,GOOGL,HOOD,MSTR,NVDA,TSLA --source finnhub:earnings:runner` on `daily(23:00Z)`; tier-2; freshness SLA 90000s; no `--from`/`--to` because EarningsArgs defaults give a rolling -30d/+90d window. First-fire 2026-05-04 21:43Z: all 6 symbols' `_fetched_at` advanced. **G3**: new `ops/sources/cme-intraday-1m.toml` runs `scry databento intraday1m --symbols ES=F,NQ=F,GC=F,ZN=F --lookback-days 2 --end-safety-margin-secs 36000 --source databento:glbx-mdp3:runner` on `daily(06:00Z)`; tier-1; freshness SLA 90000s. **Rust support for G3**: `bin/scry/src/databento_cmd.rs` `IntradayArgs` gains `--lookback-days N` (default 2) and `--end-safety-margin-secs N` (default 600), with `--start`/`--end` defaulting to "". When both are empty, `end_dt = now_utc - safety_margin` and `start_dt = end_dt - lookback_days`, both as sub-day OffsetDateTime instants (not YYYY-MM-DD) to avoid overshooting Databento's published-data horizon. **Operational fix during smoke**: first `once` fire returned 422 with `end = today + 1d` (overshot horizon); second `once` fire after end-instant fix returned 422 again with `requires a subscription and/or license` (operator's GLBX.MDP3 license is delayed-access ~8h, not realtime). Manifest tunes to `--end-safety-margin-secs 36000` (10h cushion) and `daily(06:00Z)` cadence so the prior trading day's full session is past the 8h horizon by fire time. Third `once` fire 2026-05-04 21:56Z succeeded — `year=2026/month=05/day=04.parquet` landed for all 4 symbols. **Env**: `FINNHUB_API_KEY` and `DATABENTO_API_KEY` moved from repo `.env` into deployed `~/Library/Application Support/scryer/.env` (backup at `.env.bak.20260504T173958Z`); `FRED_API_KEY` deliberately not moved because no manifest references `scry fred`. All three manifests run on the existing multi-manifest `runner-tick` plist via auto-discovery — no new launchd plists. None of these touch the Helius/Alchemy/QuickNode/RPCFast quota incident. |
| 119 | 2026-05-04 | PR.7 proxy v0.2 resilience — code-shipped, data-pending | Methodology lock `Proxy v0.2 Resilience - 2026-05-04`. Triggered by simultaneous monthly-cap exhaustion of Helius / QuickNode / Alchemy plus discovery that the RPCFast API key was revoked (server returns "API key is required" for every auth shape). Six pieces in one PR: (1) **Operator kill-switch** — `Registry::ranked_eligible` filter gains `p.config.weight > 0`; weight=0 keeps the provider registered (probes/metrics flow) but excludes it from routing. (2) **Per-provider in-flight bulkhead** — `tokio::sync::Semaphore` per `ProviderState`, default cap 32, configurable via `max_in_flight` in `providers.json`; router awaits up to `RetryConfig::bulkhead_acquire_timeout` (default 2s); on timeout treats as self-throttle (`request_failures_total{reason="bulkhead_full"}`, `retries_total{reason="bulkhead_full"}`), tries next eligible without consuming `budgeted_attempts`. (3) **Per-provider rate limit** — token bucket per provider (refill = `max_rps`, burst = `max_rps`); `try_acquire` is non-blocking; deny → `request_failures_total{reason="rate_limit_self"}` and try next. Foundation lands at `max_rps: 2` after a brief 4-rps soak still saw 502s on Foundation throttle. (4) **Recovery-probe hysteresis** — `HealthConfig::required_consecutive_ok = 2`; quarantine clears only after the threshold of consecutive Ok probes (with non-Ok resetting); 5-min recovery-probe interval × 2 = 10-min minimum re-eligibility window. New `consecutive_recovery_ok` AtomicU32 on `ProviderState`; new `record_recovery_probe_partial` helper updates EMA without clearing quarantine when the threshold isn't yet met. (5) **Adaptive `max_attempts_read`** — default bumped 2 → 5; loop walks `min(cap, eligible.len())`; CapabilityMismatch and self-backpressure still don't consume budget. (6) **`request_retry_exhausted_total` IntCounter** — bumped exactly once per inbound request that walks the full eligible list and returns 503; the canary metric for the routing-leak failure mode. `providers.json` schema gains optional `max_in_flight` and `max_rps` fields (non-breaking; existing configs default to 32 / unlimited). Helius / QuickNode / Alchemy / RPCFast all set to `weight: 0` in workspace + staged `providers.json`; Foundation gets `max_rps: 2`. dex-xstock-swaps manifest paused (`.toml.paused` + dedicated `runner-dex-xstock-swaps.plist` unloaded) because Foundation @ 2 rps cannot carry its 50+ getTransaction-per-60s burst; un-pause when at least one paid Solana RPC provider returns. 4 new unit tests (`weight_zero_excludes_provider_from_ranked_eligible`, `hysteresis_first_ok_does_not_clear`, `hysteresis_second_ok_clears`, `hysteresis_non_ok_resets_counter`); 37 unit + 13 integration tests pass. Live verification (15:00Z): 4 of 5 manifests that were actively failing pre-PR.7 (kamino-scope-tape, clmm-pool-state, dlmm-pool-state, dex-xstock-swaps) recovered after the rate-limit cap was tuned in; metrics confirm `request_failures_total{Foundation, rate_limit_self}=3096` over the soak (proxy is correctly pacing before hitting Foundation). Data-pending verification trigger logged in `wishlist.md` Data-Pending Verification: `request_retry_exhausted_total` rate < 50/hr over a 6-hour window once at least one paid provider is back at `weight=1`, AND no failing Solana-RPC manifest fires for 1 hour. |
| 123 | 2026-05-10 | 2026-05-10 forward-poll batch — Pyth Lazer fetcher, Blue Ocean OCEA backfill, F2/F3a/F3b manifests | Five wishlist rows shipped in one batch. **(1) Pyth Lazer fetcher** (wishlist row 40): new `crates/scryer-fetch-pyth-lazer` (`pyth-lazer-client = "25"`, `pyth-lazer-protocol = "0.40"`); new schema `oracle.pyth_lazer.tape.v2` keyed on `(price_feed_id, publish_timestamp_us)`, partitioned by `feed_id`; new `scry pyth-lazer subscribe` CLI with `--dry-run`; new `ops/sources/pyth-lazer-tape.toml` (`interval(60s)`, 55s/fire at `fixed_rate@200ms`) + per-manifest plist `com.adamnoonan.scryer.runner-pyth-lazer-tape.plist`; `runner-tick.plist` gains `--skip pyth-lazer-tape`. Methodology lock `Pyth Lazer ingestion — 2026-05-10`. First-fire probe Sun 2026-05-10 ~00:30Z confirmed all 13 default feeds (3 crypto + 10 equity underliers) return live prices on the free Pyth Terminal API key; runner first-fire 00:41Z wrote 3490 rows under `_source=pyth-lazer:ws:runner`. Free-tier finding: Blue Ocean ATS overnight pricing (8 PM – 4 AM ET Sun-Thu) IS in the free public feed-id space (answers soothsayer §6.1 / N1). **(2) Blue Ocean OCEA backfill** (wishlist row 41): new schema `bo_intraday_1m.v1` (mirror of `cme_intraday_1m.v1` shape, venue `blue_ocean`); new `scryer_fetch_databento::fetch_ocea_ohlcv_1m` (`Dataset::OceaMemoir` + `SType::RawSymbol`); new `scry databento ocea1m` subcommand. Operator backfill 2025-08-25 → 2026-05-10 wrote 728,306 rows / 1780 partitions / 29 MB for the 10-symbol panel at ~$0.012 in Databento credits. No forward manifest — Lazer covers live overnight, OCEA is one-shot historical only. **(3) Soothsayer F2 CME symbol expansion** (wishlist row 37): `ops/sources/cme-intraday-1m.toml` `--symbols` extended 4 → 6 (added CL=F, 6E=F); `[budget]` raised 20 → 30. **(4) Soothsayer F3a nasdaq-halts forward poll** (wishlist row 38): new `ops/sources/nasdaq-halts.toml` runs `scry rss nasdaq-halts` on `interval(3600s)`. **(5) Soothsayer F3b backed-corp-actions forward poll** (wishlist row 39): new `ops/sources/backed-corp-actions.toml` runs `scry rss backed` on `interval(3600s)`. Methodology lock `Blue Ocean ATS overnight 1m bars — 2026-05-10`. Operator action remaining: `scryer deploy` to load the new pyth-lazer plist + stage the F2/F3a/F3b manifests; first-fire triggers for rows 37/38/39 will fire within 4h of deploy per the per-row verification triggers in wishlist Data-Pending Verification. |
| 124 | 2026-05-11 | Pyth Lazer xStock panel correction — 13 → 26-feed default | Decision Index row "Pyth Lazer carries no tokenized-equity SPL feeds" retracted. Pyth Lazer DOES carry direct xStock feeds; the 2026-05-10 catalog scan missed them because they live under `Crypto.<TICKER>X/USD` (`asset_type=crypto`, uppercase X) — not the `Token.SPYx/USD` / `xStock.*` / lowercase-`SPYx` patterns the prior scan searched. Re-verified 2026-05-11 against the same public `pyth.dourolabs.app/v1/symbols` endpoint: 28 XSTOCK-described feeds (13 stable `*X/USD` including `Crypto.SPYX/USD` = feed-id **1843**, 13 stable `*X/<TICKER>.RR` redemption-rate twins, 1 coming_soon MSFTX, 1 inactive AMBRX). New `pub const DEFAULT_XSTOCK_SYMBOLS: &[&str]` in `scryer-fetch-pyth-lazer` covering the 13 stable `*X/USD` feeds (SPYX, QQQX, AAPLX, GOOGLX, NVDAX, TSLAX, MSTRX, HOODX, METAX, COINX, CRCLX, MCDX, NFLXX). `bin/scry/src/pyth_lazer_cmd.rs` chains the new const into the default subscription so the runner picks them up after `scryer deploy`. `ops/sources/pyth-lazer-tape.toml` description + comment block + `[budget] max_requests_per_run` (50 → 100) updated for the 26-feed panel. Methodology entries updated: Decision Index gains a 2026-05-11 retraction row "Pyth Lazer xStock feeds live under `Crypto.<TICKER>X/USD`"; the prose bullet in the `Pyth Lazer Ingestion — 2026-05-10` section is rewritten to enumerate the actual feed namespace and panel composition; the anti-rule against raising cadence updates the panel-size reference from 13 → 26. `wishlist.md` row 41 (Blue Ocean OCEA backfill) framing corrected — OCEA-as-historical-only rationale preserved, but the false "Lazer carries zero tokenized-equity feeds" premise removed. `cargo check -p scryer-fetch-pyth-lazer -p scry-bin` clean; `cargo test -p scryer-fetch-pyth-lazer --lib` 2/2 pass. Data-pending verification trigger: first-fire under the 26-feed panel writes ≥1 `oracle.pyth_lazer.tape.v2` row per `feed_id={1843, 1792, 1808, 1833, 1847, 1827, 1815, 1824, 1796, 1798, 1822, 2685, 1837}` (the 13 xStock feed-ids) within 4h of `scryer deploy`, all under `_source=pyth-lazer:ws:runner`. |

## Phase Ledger

| Phase | Date | Artifact |
|---|---|---|
| 1 | 2026-04-27 | workspace scaffold; `swap.v1`; `trade.v1` |
| 2 | 2026-04-27 | `scryer-store` parquet writer/dedup |
| 3 | 2026-04-27 | `scryer-proxy` v0.1 |
| 4 | 2026-04-27 | Raydium v4 swap fetcher |
| 5 | 2026-04-27 | `scry` import/live CLI |
| 6-15 | 2026-04-27/28 | soothsayer import schemas through `kraken_funding.v1` |
| 16 | 2026-04-28 | wishlist and Priority-0 schema locks |
| 17-19 | 2026-04-28 | original Priority-0 trilogy schemas/fetchers |
| 20-21 | 2026-04-28 | soothsayer daemon migration follow-ups |
| 26 | 2026-04-28 | remaining soothsayer daemon migration |
| 28b-35 | 2026-04-28 | Kamino reserve, Jito, oracle, borrower books, equity/macro/news |
| 36-38 | 2026-04-28 | DEX xStock swaps, Pyth publisher, CME 1m |
| 39-47 | 2026-04-28/29 | Databento fixes, Drift/Mango/funding/fees/volatility/macro |
| 48-52 | 2026-04-29 | quant-work support, holders, EDGAR, EVM liquidations |
| 52-58 | 2026-04-29 | Pyth poster and CEX stock-perp work |
| 59-63 | 2026-04-29/30 | Backed NAV, Chainlink, corp actions, halts, CME backfill |
| 64-74 | 2026-04-30/05-01 | poster tx, cadence, watchdog, MarginFi, Pyth backfill, proxy/deploy, Kraken trades |
| 77-84 | 2026-05-01 | LVR pivots, Paper-4 specs, v0.2 platform methodology |

## Append Rule

New rows should be short. Put design rationale in `methodology_log.md`, schema fields in `docs/schemas.md`, and forward work in `wishlist.md`.
