# Rust
target/
**/*.rs.bk
*.pdb
# Cargo.lock is committed: the workspace ships binaries
# (bin/scryer-proxy, future bin/scry). See "Cargo.lock policy" in
# methodology_log.md.

# Data — rebuildable from upstream sources, never tracked
dataset/
data/
*.parquet
*.parquet.tmp

# Secrets
.env
.env.local

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp
.claude/

# Logs
*.log
logs/

# JS / Tauri build artifacts (scryer-portal-shell/ui)
node_modules/
dist/
*.tsbuildinfo
.vite/
