# scryer Wish List

Forward work only. Shipped narrative belongs in `docs/phase_log.md`; schema fields belong in `docs/schemas.md`; architectural rationale belongs in `methodology_log.md`.

Last compacted: 2026-05-02.

## Active Priorities

| Priority | Item | Status | Next Action |
|---|---|---|---|
| — | 47 `marginfi_liquidation.v1` | **Done** — phase 113 (2026-05-04). 677 rows landed across `[2026-05-01, 2026-05-03]`. Follow-on items in **Item 47 follow-ons** below: (47.1) inner-IX SPL-Transfer walk for native-unit fees — **Done phase 116 (2026-05-04)**, code-shipped + smoke-validated; existing canonical partitions still carry zeros until operator deletes and re-fetches; (47.1.b) cross-IX withdraw walk for the gross asset seizure under flashloan-arb (carved out from 47.1, deferred); (47.2) health-value scale interpretation — **Done phase 117 (2026-05-04)**, doc-only fix pinning the maintenance-weighted USD-equivalent formula against marginfi-v2 source, sub-zero = liquidatable; (47.3) `marginfi_reserve.v1` symbol map. None block Item 47 itself; they sharpen the data quality. |
| P1.5 | 49 oracle coverage-inversion historical panel | partially shipped | Finish 49a operator run; implement/run 49b-49d. |
| P2 | 55 `us_macro_calendar.v1` — FOMC + CPI release dates | **requested** (soothsayer, 2026-07-25) | Two small static/near-static public calendars: FOMC statement release datetimes (Fed press-release schedule, 2014→) and BLS CPI release datetimes (BLS release calendar, 2014→). Both are scheduled years ahead, so this is a backfill + thin forward runner, not a poll. **Why:** soothsayer W12/W13 established that its regime partition is load-bearing *specifically* for events knowable from the calendar before they occur — no adaptive method can widen a band for an unobserved scheduled event (overnight `earnings_night`: partition 0.983 vs every online-conformal arm 0.317–0.383). Adding calendar cells is therefore the highest-value architecture work available, and FOMC/CPI are the two remaining high-impact candidates. Triple witching was implementable in-repo (purely calendrical, third Friday of Mar/Jun/Sep/Dec) and already found a live 0.869-vs-0.95 coverage hole; FOMC and CPI need real release *dates and times*, which is upstream data and therefore blocked on this item per soothsayer CLAUDE.md rules #1/#2. **Consumer shape:** a `regime_pub` cell on the overnight panel, so what matters is the release datetime in ET and which overnight gap it lands in (analogous to the earnings.v2 bmo/amc single-gap mapping). Intraday timing matters — CPI is 08:30 ET pre-open, FOMC statements are 14:00 ET intraday with the reaction spilling into the following close→open gap. |
| — | 59 `clmm_pool_registry.v1` — pool → mint/decimals/fee-tier identity | **Done** — shipped 2026-07-26. Schema + `scryer-fetch-solana::clmm_pool_registry` + `scry solana clmm-pool-registry`; 40 rows across 2 `dex=` partitions on first fire, re-run idempotent (0 added / 40 deduped). Decode validated against live data, not just unit tests: USDC at 6 decimals, wrapped SOL at 9, xStocks at 8, canonical Orca/Raydium fee tiers, and tick-spacing↔fee-tier pairing emerging correctly. End-to-end join gives SPYx at 744.56–746.30 across its four fee tiers, matching an independent Jupiter/CEX read of 746.08. **Operator step remaining:** the installed `bin/scry` predates this command, so the `clmm-pool-registry` manifest is written to `ops/sources/` but NOT deployed — deploying needs a `scry` rebuild+install, which is a separate call because the tree is dirty. Until then the registry is refreshed manually and its staleness is watched by quant-work's liveness gate. | Deploy `ops/sources/clmm-pool-registry.toml` once `scry` is rebuilt and installed to `bin/` — that install is the only thing between this and a self-refreshing daily row. Until then the registry is manual and its staleness is covered by quant-work's liveness gate. Also consider re-running after item 60 widens the pool list. |
| P2 | 60 add SOL/USDC + majors to the CLMM pool list — start the clock | **requested** (quant-work, 2026-07-26) | `ops/sources/data/clmm-pools.txt` is xStock-seeded ("CLMM pools touching xStock mints", generated 2026-05-02 from GT discovery over 5 of 8 mints before rate-limiting). Consequence: the 85-day, 70,631-snapshot-per-pool state tape covers **only** tokenized equities, and there is no CLMM pool for SOL/USDC or any other deep major. Any future LVR/AMM study wanting a deep pool with a clean, live 24/7 external reference (`kraken/trades` SOLUSD, 1.67M rows and current) has to start accumulating from zero. **Why now, and it is the whole point:** LVR-01 died precisely because nothing was accumulating while the project waited on an infeasible historical backfill; the banked xStock panel only exists because someone happened to start it in May. Adding pools is a text-file edit plus a regenerate, costs one extra `getMultipleAccounts` slot per fire, and converts a 3-month future wait into a 3-month-old dataset. Cheap option value on a surface that is already running. Also fill the 3 xStock mints GT rate-limited out of the original discovery. |
| P1 | 61 CLMM tick-range / liquidity-at-tick distribution | **requested** (quant-work, 2026-07-26) | `clmm_pool_state.v1` carries `tick_current` and `liquidity` but not the distribution of liquidity across ticks, so there is no way to know the *range* over which the active `L` is valid. **Why it blocks real work:** quant-work LVR-02's discovery run (2026-07-26) returned a pooled realized/predicted ratio of 0.0319 and was found to be a specification defect rather than a result — 66.1% of all predicted LVR came from a single pool-day. The CL LVR rate `ℓ = (σ²/4)·L·√P` uses `L·√P`, the *virtual* token1 reserve, which for a tight range is enormous without corresponding capital: one SPYx Whirlpool went 4.2e11 → 3.2e15 liquidity over 2026-06-14/15 (a real, gradual build-and-decay, not a bad decode), implying 8.86bn USDC of virtual notional on an xStock pool against a panel median of 1.56m. Integrating the instantaneous rate over a snapshot interval assumes L stays in range for that whole interval, which is false for a tight range — and the in-range duration is exactly what cannot be computed today. **Shape:** the tick bitmap plus per-initialized-tick `liquidity_net`, enough to reconstruct the active range around `tick_current` and therefore how far price can move before `L` changes. Both programs expose it via tick-array accounts (Whirlpool `TickArray`, Raydium CLMM `TickArrayState`), so it is more `getMultipleAccounts` on accounts derivable from the pool address and tick spacing — no new provider, no new auth. Cost is the open question: tick arrays are large and there are several per pool, so this likely wants a lower cadence than the 60s state tape, or capture only arrays adjacent to `tick_current`. |
| P2 | 53 `nasdaq_halts_intraday.v1` | code-shipped + runner-live | Wait for first halt within Yahoo's 7d horizon to land bars; promote to Done when one halt event has captured bars in canonical dataset. |
| P3 | 42 soothsayer relay scaffold | in flight, cross-repo | Coordinate with soothsayer; do not treat as scryer-only. |
| P3 | 43 relay daemon mirror tape | in flight, cross-repo | Depends on item 42. |
| — | 54 `oracle.soothsayer_v6.band_tape.v2` | **paused 2026-05-03** — see Blocked / Gated | Soothsayer-side methodology under revisit; publisher daemon may be redone. Do not invest further on this item until the new methodology lands. |
| P4 | 36 `dex_treasury_swaps.v1` | gated | Wait for multi-class scope decision. |
| P4 | 38 `treasury_auction.v1` | gated | Wait for multi-class scope decision. |

## Data-Pending Verification

Code shipped, canonical data still landing or pending. Per Hard Rule #9 in `CLAUDE.md`, items here cannot flip to Done until the verification trigger fires. Add an entry any time a change ships code without landing canonical rows. Promote out — move to **Recently Shipped Pointers** and log a `docs/phase_log.md` row — once the trigger fires.

| Item | What was shipped | Verification trigger | Status / next check | Started |
|---|---|---|---|---|
| 56 cboe-indices fail-fast + retry budget | 2026-07-25. Two defects found when soothsayer's new Saturday band-commitment aborted on a missing `^VIX` bar for Fri 2026-07-24. **(a) Fail-fast:** `bin/scry/src/cboe_cmd.rs` used `?` on each `fetch_index_history`, and `VIX` is first in `SUPPORTED_INDICES` — so a single transient failure on VIX wrote ZERO rows for all six indices. Observed on 2026-06-26, 07-01, 07-22, 07-25, every one a `dns error: failed to lookup address information` on `cdn.cboe.com`. Now per-index isolated: successful indices are written, then the command exits non-zero so the runner still records `failed` + a dead_letter row (salvage the data, keep the alarm). **(b) Retry budget:** manifest now passes `--retry-max 5 --retry-delay-secs 10` (50s of tolerance) vs the 3×2s=6s default, which cannot ride out a DNS flap. Each fire is a full-history idempotent re-pull, so a slower run costs nothing. Recovery for the 07-24 gap was a manual `--indices VIX,SKEW` re-pull (`rows_added=2`, `_source=cboe:csv:manual-recover`) plus a forced `once` fire; all 6 indices now current through 2026-07-24. 45 tests pass; partial-failure exit=1 and all-ok exit=0 both verified. | Next natural `cdn.cboe.com` transport failure records `failed` **and** still advances `_fetched_at` for the non-failing indices — i.e. a dead_letter row whose message reads `N/6 index fetches failed (rows for the rest were written)`. | awaiting-event — passive wait for the next DNS flap | 2026-07-25 |
| 57 CBOE VIX/SKEW publication lag vs the 22:30Z fire | 2026-07-25, **not yet fixed — diagnosis only.** Friday 2026-07-24's fire *succeeded* at 18:30 ET but `VIX` and `SKEW` came back stale (max date 07-23) while `VIX1D/3M/6M/9D` all carried 07-24. CBOE republishes the VIX and SKEW history CSVs later than the rest of the family, and phase 121 sized the `daily(22:30Z)` cadence off a "~2h post-16:15 ET settlement" assumption — that leaves ~15 min of margin and it now demonstrably slips. The architecture normally self-heals (the *next* day's 18:30 ET fire catches the prior close, 1h before soothsayer's Sat 19:30 ET band-commitment), which is why this went unnoticed — but item 56's DNS failure landed on exactly that recovery fire and the two compounded into a missed weekend commitment. Options not yet taken: shift the sensor later (the `daily(<HH:MM>Z)` grammar is single-time and locked, so this is a straight swap, but a late-evening fire risks machine sleep), or widen soothsayer's commitment margin. | A decision recorded here plus, if the sensor moves, 5 consecutive fires landing same-day `VIX` closes. | **needs a call from Adam** — 1h of margin currently stands between a single failed fire and a missed weekend commitment | 2026-07-25 |
| 58 freshness/coverage watchdogs are blind to stale-but-fetched data | 2026-07-25, **not yet fixed — diagnosis only.** Neither existing watchdog could have caught items 56/57. `[freshness] sla_secs` measures *when the fetch ran*, not *how recent the newest row is* — a run that succeeds and returns a stale CSV passes cleanly. `ops/coverage_gap_detector.py` explicitly excludes the backfillable daily sources (`equities`, `cboe`, `nasdaq`, `cme`) by design. Net: a silently stale daily source is invisible until a downstream consumer aborts, which is precisely how this surfaced — soothsayer's band-commitment was the detector. Proposed shape: a `max(date)`-recency check for daily sources, expressed in trading days rather than wall-clock so weekends/holidays don't false-alarm. **⚠️ 2026-07-26 (quant-work): the blind spot is wider than diagnosed, and it settles the design question.** A liveness audit over the whole dataset root found **13 of 40 venue/data_type surfaces dead >7 days**, nine of them dying inside 2026-04-29 → 2026-05-06 — the M3.x manifest/runner migration window (phases 92–121). Eleven of the thirteen have **no manifest and no launchd job at all**: `kraken/funding` (Apr 29), `jupiter_lend/vault_configs` (Apr 29), `kamino/reserves` (Apr 29), `kamino/liquidations` (Apr 30), `cex_stock_perp/ohlcv` (May 1), `marginfi/liquidations` (May 3), `marginfi/reserves`, `blue_ocean/intraday_1m` (May 10), `yahoo/corp_actions` (Jul 9), plus `chainlink/data_streams` (deferred manifest). They were not migrated, their old plists went away, and nothing replaced them. The remaining two — `solana_raydium_v4/swaps` (May 4) and `pool_snapshots` (May 6) — still have plists, still run, and still exit 1 on every invocation with RPC `503 no healthy providers` / `502 rate-limited by proxy`; that pair silently killed quant-work's LVR-01 for 83 days. Meanwhile the freshness watchdog has alerted on exactly five sources in its entire history (`geckoterminal`, `kamino-scope`, `pyth`, `redstone`, `v5`) — **all five currently live** — and has never once alerted on any of the thirteen dead ones. **Design implication:** the watch list is derived from manifests, so a source that loses its manifest simultaneously loses its monitoring — the monitor is derived from the thing being monitored and is structurally blind to the failure that actually happens. The fix must therefore **iterate the dataset root on disk, not the manifest list**, and treat "a surface exists with rows but has no owning manifest" as itself an alertable condition. That single change covers both the stale-but-fetched case (56/57) and the orphaned-surface case above. | A watchdog that would have alerted on `cboe/indices/v1/index=VIX` at 18:31 ET on 2026-07-25, **and** on `kraken/funding/v1` at any point in the 87 days since it stopped. | proposed — not scoped; scope now includes orphan detection | 2026-07-25 |
| 49a Pyth Benchmarks 180d backfill | Code phase 71; operator backfill launched 2026-05-03 | Continuous `pyth/oracle_tape/v1/` partitions across `[2025-11-04, 2026-05-03)` with non-empty rows under `_source=pyth:hermes:benchmarks` | running — PID 67593, log `~/Library/Logs/scryer/pyth-backfill-180d-20260503T214657Z.log`; ~18h wall-clock at 4 req/s | 2026-05-03 21:46Z |
| 51e historical backfill (operator job, separate from wishlist item 51e itself) | Forward-poll runner shipped phase 101; range backfill is a separate operator job | Partitions across `[2025-07-14, 2026-04-28]` for all 8 xStock symbols at `dex_xstock/swaps/v1/symbol={…}/…` under `_source=helius:parseTransactions:backfill` | running — PID 67608, log `~/Library/Logs/scryer/dex-xstock-swaps-backfill-20260503T214659Z.log`; stage-1 sig pagination on SPYx | 2026-05-03 21:46Z |
| 53 `nasdaq_halts_intraday.v1` | Code + runner shipped phase 109 (2026-05-03) | ≥1 halt event from `nasdaq_halts.v1` within Yahoo's 7d horizon produces non-empty 1m bars at `nasdaq/halts_intraday/v1/` | awaiting-event — last known halts were 2026-04-24, already outside horizon | 2026-05-03 |
| 52 `volatility.yahoo.single_stock_iv.v2` | Forward-poll runner shipped phase 107 (2026-05-03); first canonical fire same day | (a) ~20 forward weekends accrued in `volatility.yahoo/single_stock_iv/v2/`, OR (b) paid-venue sibling manifest covering 2014→ | awaiting-accrual — 1 weekend captured, ~19 to go | 2026-05-03 |
| 49d `chainlink_data_streams.v1` ≥90d historical | Forward-poll runner-live; historical range backfill not yet launched | 90d of `chainlink/data_streams/v1/` partitions across the chosen range, non-empty | awaiting-launch — gated on a 7d spike test before 90d commitment, due to Verifier-program RPC volume | n/a |
| 54 `oracle.soothsayer_v6.band_tape.v2` | Code + manifest shipped phase 110 (2026-05-03); manifest paused same day (renamed `.toml.paused`, staged copy removed) so runner-tick is no longer firing. Schema/fetcher/CLI remain in tree | Resume only after soothsayer-side methodology revisit lands. If the post-A4 wire format is preserved the existing v2 schema works as-is; if profile/regime/wire semantics change, the resume bump is `oracle.soothsayer_v6.band_tape.v3` (or a new venue), not a v2 row | **paused — methodology-dependent** (see Blocked / Gated) | 2026-05-03 |
| PR.7 proxy v0.2 resilience | Phase 119 (2026-05-04). Methodology lock `Proxy v0.2 Resilience - 2026-05-04`. Bulkhead + rate limit + probe hysteresis + scaled retry budget + `request_retry_exhausted_total` metric + `weight=0` operator kill-switch. Live in production proxy. dex-xstock-swaps manifest paused (`.toml.paused`, dedicated runner plist unloaded) because Foundation @ 2 rps cannot carry 50+ getTransaction/60s; un-pause when at least one paid provider returns | (a) `request_retry_exhausted_total` rate < 50/hr over a 6-hour window once at least one paid Solana RPC provider is back at `weight=1`, AND (b) no failing Solana-RPC manifest fires for 1 hour over the same window. Until then PR.7 is exercised but degraded by the broader provider-exhaustion incident: Helius / QuickNode / Alchemy at monthly cap, RPCFast key revoked, only Foundation @ `max_rps=2` active | awaiting-paid-provider — Helius / QuickNode / Alchemy waiting on monthly cycle (~2026-05-31 boundary), RPCFast pending key rotation by operator. dex-xstock-swaps un-pause is part of the same trigger | 2026-05-04 |
| M6 G1 — equities-daily symbol expansion | Phase 120 (2026-05-04). `ops/sources/equities-daily.toml` `--symbols` extended from 10 → 11: added BTC-USD (load-bearing for MSTR's post-2020-08 factor pivot). First-fire smoke verified 2026-05-04 21:42Z — all 11 symbols' `_fetched_at` advanced. The brief's other 5 lining symbols (ES=F, NQ=F, GC=F, ZN=F, ^VIX) were tested first and found to return `No data` / zero rows on Stooq's free-tier CSV endpoint — see G1.b for the open work. **Done — code + canonical data** | First-fire trigger fired: 11/11 symbols' `_fetched_at` advanced post-deploy at 2026-05-04 21:42Z. | done | 2026-05-04 |
| M6 G1.b — futures/^VIX daily forward poll (gap surfaced by G1) | Phase 121 (2026-05-04). Resolved with split decision per soothsayer agent: **VIX served by new CBOE manifest**; **futures served by soothsayer-side resample of `cme/intraday_1m/v1`**. New `ops/sources/cboe-indices.toml` runs `scry cboe indices --source cboe:csv:runner` on `daily(22:30Z)`, ships VIX/VIX9D/VIX1D/VIX3M/VIX6M/SKEW under `dataset/cboe/indices/v1/index={IDX}/year={YYYY}.parquet`. First-fire 2026-05-04 21:58Z: 6/6 indices populated; VIX 2026 partition = 85 rows through 2026-05-01 (Fri close; today's Mon close settles 16:15 ET / 20:15 UTC and propagates within ~2h, so the planned daily(22:30Z) fire is the right cadence going forward). ES=F/NQ=F/GC=F/ZN=F daily bars NOT shipped via a new scryer fetcher — soothsayer resamples the existing `cme/intraday_1m/v1` to daily consumer-side per the M6 hand-off accepted contract. ^GVZ/^MOVE remain on the soothsayer-side `^VIX` fallback per brief §5 watch-out #1 (CBOE free CSV doesn't serve them; ICE doesn't expose ^MOVE freely). **Done — code + canonical data.** | First-fire trigger: 6/6 CBOE indices' partitions populated and `_source=cboe:csv:runner` ✓ 2026-05-04 21:58Z. Post-promotion check: VIX 2026 partition advances on next daily(22:30Z) fire to include 2026-05-04 close | done | 2026-05-04 |
| M6 G2 — earnings forward-poll manifest | Phase 120 (2026-05-04). New `ops/sources/earnings.toml`: 6 symbols (AAPL, GOOGL, HOOD, MSTR, NVDA, TSLA) via Finnhub, `daily(23:00Z)`. Replaces operator-fed cadence so Soothsayer's `earnings_next_week` regime flag does not silently misclassify post-freeze weekends. First-fire smoke verified 2026-05-04 21:43Z — all 6 symbols' `_fetched_at` advanced. **Done (code + canonical data) for the manifest stand-up.** **Phase 126 (2026-05-25): upgraded to `earnings.v2` with session timing — runner now emits `session` from Finnhub's `hour`; one-shot Yahoo `earnings-backfill` landed deep history (AAPL→1993) with ET-derived bmo/amc; `earnings-migrate` lifted v1 into v2. 100% timed among reported earnings 2015+. Known permanent gaps (not actions): pre-2015 rows carry dates but `session=unknown` (Yahoo supplies no times that far back); future quarters beyond the Finnhub +90d / Yahoo windows stay `unknown` until the daily runner reaches them.** Stricter "≥1 new earnings event in a 30-day window" promotion criterion remains open; that's a wait-for-event window, not an action. | (a) first-fire trigger: 6/6 symbols' `_fetched_at` advanced 2026-05-04 21:43Z ✓; (b) ≥1 new earnings event lands within a 30-day window from manifest go-live; (c) v2 session timing landed + spot-checked 2026-05-25 ✓ | done (code + first-fire + v2 timing) — earnings-event accrual is a passive wait | 2026-05-25 |
| M6 G3 — cme-intraday-1m forward-poll manifest | Phase 120 (2026-05-04). New `ops/sources/cme-intraday-1m.toml`: 4 symbols (ES=F, NQ=F, GC=F, ZN=F) via Databento GLBX.MDP3, `daily(06:00Z)`, `--lookback-days 2`, `--end-safety-margin-secs 36000`. New `--lookback-days` and `--end-safety-margin-secs` flags added to `scry databento intraday1m`. The 36000s (10h) safety margin matches the operator's delayed-access GLBX.MDP3 subscription tier (~8h publication lag); querying past the horizon returns 422. Cadence shifted from `daily(22:30Z)` to `daily(06:00Z)` so the prior trading day's full session is past the delayed horizon by fire time. First-fire smoke verified 2026-05-04 21:56Z (`once` force-fire after the safety-margin fix) — new day partitions for `2026-05-04` landed for all 4 symbols. Optional in the M6 forward-tape battery — gated on Soothsayer keeping §6.6 path-fitted in the harness. **Done (code + canonical data).** | First-fire trigger: a new day partition past `2026-04-28` for all 4 symbols ✓ — `year=2026/month=05/day=04.parquet` written 2026-05-04 21:56Z under `_source=databento:glbx-mdp3:runner` | done | 2026-05-04 |
| Soothsayer F2 — cme-intraday-1m CL=F + 6E=F symbol expansion | 2026-05-10. `ops/sources/cme-intraday-1m.toml` `--symbols` extended from 4 → 6: added CL=F (NYMEX crude) and 6E=F (CME Euro FX) per soothsayer's intraweek-overnight panel ask (factors flagged in `soothsayer.universe.EXOGENOUS_EQUITY`). `symbol_to_databento_continuous` already generic (strips `=F`, appends `.v.0`); test extended to assert CL.v.0 / 6E.v.0 mappings. Manifest `[budget]` raised 20 → 30 to match. **Code-shipped — operator action needed: `scryer deploy` then verify first fire writes day partitions for both symbols.** | First-fire trigger: a `cme/intraday_1m/v1/symbol={CL=F,6E=F}/year=2026/month=*/day=*.parquet` partition writes within 24h of `scryer deploy`, with `_source=databento:glbx-mdp3:runner`. If either returns zero rows on first fire, drop from `--symbols` and file a follow-up wishlist row for an alternative continuous-contract syntax. | awaiting-deploy | 2026-05-10 |
| Pyth Lazer xStock panel expansion (13 → 26 feeds) | Phase 124 (2026-05-11). Retracts the 2026-05-10 "Pyth Lazer carries no tokenized-equity SPL feeds" methodology entry. Pyth Lazer DOES carry direct xStock feeds under `Crypto.<TICKER>X/USD` (`asset_type=crypto`, uppercase X); the prior scan missed them by searching the wrong namespaces. New `DEFAULT_XSTOCK_SYMBOLS` adds the 13 stable `*X/USD` feeds (SPYX = feed-id 1843, QQQX, AAPLX, GOOGLX, NVDAX, TSLAX, MSTRX, HOODX, METAX, COINX, CRCLX, MCDX, NFLXX). Manifest description + budget updated; `cargo check` clean. **Done — code + canonical data.** Deployed 2026-05-11 14:23Z; first-fire under the 26-feed panel wrote 268 rows/feed for all 13 xStock feed_ids in the first 55s cycle (fixed_rate@200ms cadence confirmed). Sample last-prices: SPYX 742.54, QQQX 714.11, NVDAX 219.58, TSLAX 420.26, AAPLX 293.64, GOOGLX 395.60, METAX 604.90, MSTRX 188.87, HOODX 77.75, COINX 205.72, CRCLX 116.85, MCDX 278.10, NFLXX 87.54 — all live overnight prices during the Blue Ocean ATS window. | First-fire trigger fired ✓ 2026-05-11 14:23Z — all 13 xStock feed_ids landed canonical partitions at `oracle.pyth_lazer/tape/v2/feed_id={1792,1796,1798,1808,1815,1822,1824,1827,1833,1837,1843,1847,2685}/year=2026/month=05/day=11.parquet` under `_source=pyth-lazer:ws:runner`. | done | 2026-05-11 |
| Soothsayer F3a — nasdaq-halts forward-poll manifest | 2026-05-10. New `ops/sources/nasdaq-halts.toml` runs `scry rss nasdaq-halts` on `interval(3600s)`. Replaces the operator-fed cadence on the halts table itself — last fetch was 2026-04-26 (14d stale at write time), invalidating the soft dep from `nasdaq-halts-intraday` and from soothsayer F3 ask. Idempotent dedup on `(symbol, halt_date, reason_code)`. **Code-shipped — operator action: `scryer deploy` then verify first fire advances `_fetched_at` on `nasdaq/halts/v1/year=2026.parquet`.** | First-fire trigger: `_fetched_at` on `nasdaq/halts/v1/year=2026.parquet` advances past `2026-04-26 09:14Z` within 4h of deploy. | awaiting-deploy | 2026-05-10 |
| Soothsayer F3b — backed-corp-actions forward-poll manifest | 2026-05-10. New `ops/sources/backed-corp-actions.toml` runs `scry rss backed` on `interval(3600s)`. Replaces the operator-fed cadence on the corp-actions GitHub commit feed — last fetch was 2026-04-26 (14d stale at write time). Idempotent dedup on `(repo, commit_sha)`. **Code-shipped — operator action: `scryer deploy` then verify first fire advances `_fetched_at` on `backed/corp_actions/v1/year=2026.parquet`.** | First-fire trigger: `_fetched_at` on `backed/corp_actions/v1/year=2026.parquet` advances past `2026-04-26 09:16Z` within 4h of deploy. | awaiting-deploy | 2026-05-10 |
| Pyth Lazer fetcher (`oracle.pyth_lazer.tape.v2`) | 2026-05-10. New `crates/scryer-fetch-pyth-lazer` (depends on `pyth-lazer-client = "25"` + `pyth-lazer-protocol = "0.40"`); new schema `oracle.pyth_lazer.tape.v2` keyed on `(price_feed_id, publish_timestamp_us)`, partitioned by `feed_id`; new CLI `scry pyth-lazer subscribe` with `--dry-run` probe mode; new `ops/sources/pyth-lazer-tape.toml` manifest (`interval(60s)`, 55s per fire at `fixed_rate@200ms`); new per-manifest plist `com.adamnoonan.scryer.runner-pyth-lazer-tape.plist` (`runner-tick.plist` `--skip pyth-lazer-tape` updated). Methodology lock `Pyth Lazer ingestion — 2026-05-10`. **First-fire probe 2026-05-10 ~00:30Z (Sun 8:30 PM ET) confirmed all 13 default feeds (3 crypto + 10 equities) returned live prices on the free tier with realistic spreads — Blue Ocean ATS overnight equity pricing (8 PM – 4 AM ET Sun-Thu) IS in the free public feed-id space. Wrote 1869 rows / 13 partitions across SPY/QQQ/AAPL/GOOGL/NVDA/TSLA/MSTR/HOOD/GLD/TLT + BTC/ETH/SOL with non-null signed Solana payloads (~150 bytes/row).** Runner-driven first fire 2026-05-10 ~00:41Z wrote 3490 rows under `_source=pyth-lazer:ws:runner` — fully promoted to **Done.** | First-fire trigger fired ✓ 2026-05-10 00:41:08Z (3490 rows under `_source=pyth-lazer:ws:runner`, status=succeeded, duration_ms=55000). | done | 2026-05-10 |
| Blue Ocean ATS overnight backfill (`bo_intraday_1m.v1`) | 2026-05-10. Companion to the Pyth Lazer forward tape — same Sun-Thu 8 PM – 4 AM ET window, 8.5 months of historical depth from Databento `OCEA.MEMOIR`. The original 2026-05-10 framing claimed Pyth Lazer carried zero tokenized-equity feeds; that was wrong and retracted 2026-05-11 — Pyth Lazer DOES carry direct `Crypto.<TICKER>X/USD` xStock feeds (e.g. `Crypto.SPYX/USD` = feed-id 1843), the prior scan missed them because they live under `asset_type=crypto` not `equity`. The OCEA backfill rationale still holds: Lazer's catalog only covers the live-forward window, so historical 1m bars for the Sun-Thu 8 PM – 4 AM ET Blue Ocean ATS overnight period still need Databento `OCEA.MEMOIR` as a one-shot source. The 10-symbol equity-underlier panel here matches the Lazer equity-underlier subscription set 1:1; the 13 stable `*X/USD` xStock feeds Lazer also carries are direct-token coverage and have no OCEA counterpart (OCEA is the equity-venue tape, not the SPL-token DEX tape). **Code-shipped + canonical data landed.** New schema `bo_intraday_1m.v1` (mirror of `cme_intraday_1m.v1` shape, separate venue `blue_ocean`); new `scryer_fetch_databento::fetch_ocea_ohlcv_1m` (Databento `Dataset::OceaMemoir` + `SType::RawSymbol`); new `scry databento ocea1m` subcommand. Operator backfill 2025-08-25 → 2026-05-10 wrote 728,306 rows / 1780 partitions / 29 MB on disk for the 10-symbol panel (SPY/QQQ/AAPL/GOOGL/NVDA/TSLA/MSTR/HOOD/GLD/TLT). Per-symbol rows: NVDA 85k, TSLA 85k, QQQ 78k, GOOGL 77k, MSTR 76k, GLD 73k, AAPL 74k, HOOD 71k, SPY 68k, TLT 40k (TLT lower = bond ETF / lower overnight liquidity). Cost: ~$0.012 in Databento credits at $0.40/GB. **No forward manifest** — Lazer covers live overnight; OCEA is one-shot historical only. Future top-ups: re-run `scry databento ocea1m --start <last+1> --end <today-12h>` weekly or as needed. | Backfill canonical data trigger fired ✓ — 728k rows landed across 218 distinct days for all 10 symbols 2025-08-25 → 2026-05-08, with `_source=databento:ocea-memoir:backfill`. | done | 2026-05-10 |

## Item 47 - `marginfi_liquidation.v1`

Done — phase 113 (2026-05-04). 677 rows in canonical partitions across `[2026-05-01, 2026-05-03]`. The first-live-tx validation surfaced three follow-ons that sharpen data quality without blocking Item 47 itself:

### Item 47 follow-ons

| Follow-on | What | Why |
|---|---|---|
| 47.1 inner-IX SPL-Transfer walk | **Done — phase 116 (2026-05-04).** Per-IX walk of `lending_account_liquidate.inner_instructions` ships native-unit `asset_amount_seized` (sum where `source == accounts[7]` `bank_liquidity_vault`) and `insurance_fund_fee_paid` (sum where `destination == accounts[8]` `bank_insurance_vault`). New `HeliusInstruction.parsed: Option<ParsedIxInfo>` carries the jsonParsed `{type, info}` block; `get_transactions::convert_ix` reshapes permissively. Smoke 2026-05-02 (37 liquidations): 100% non-zero population both columns; replaces the wallet-delta heuristic that returned 0 for the dominant flashloan-arb pattern. Item 47.1.b carved out: the same-tx `lending_account_withdraw` IX carries the gross seizure transfer; cross-IX attribution back to the matching liquidate IX is a future scope when data quality demands it. Operator action remaining: delete and re-fetch `[2026-05-01, 2026-05-03]` partitions to backfill 0-valued rows. |
| 47.2 health-value scale | **Done — phase 117 (2026-05-04).** Doc-only fix. Pinned the actual formula against mrgnlabs/marginfi-v2 commit `843aa82d` `programs/marginfi/src/state/marginfi_account.rs::check_pre_liquidation_condition_and_get_account_health`: `account_health = assets − liabs` over `RiskRequirementType::Maintenance`, i.e. `asset_value_maint − liability_value_maint`, both `WrappedI80F48` "* In dollars" per the `HealthCache` IDL doc strings. **Sub-zero = liquidatable**; the on-chain pre-liq gate rejects `health > 0` with `HealthyAccount`. Post-liquidation asserts `health <= 0` AND `health > pre_health`, consistent with the observed empirical `pre ∈ [-107.0630, 0.0000]` / `post ∈ [-41.4272, 0.0000]` ranges on the 677-row canonical sample. Three doc surfaces updated to match: schema field docstrings (`crates/scryer-schema/src/marginfi_liquidation.rs`), `docs/schemas.md` `## marginfi_liquidation.v1` row-block, and the `MarginFi-v2` entry in `methodology_log.md`. No code change, no schema bump; the data on disk is correct, only the prior "sub-1.0" docstring was wrong. A normalized-ratio derivation was considered and skipped — the correct USD-weighted scale is what consumers want; renormalizing throws away the magnitude information. | Consumers reading the column today will misinterpret. Also helps the `oracle_context.v1` joins by giving them an unambiguous semantic. |
| 47.3 marginfi_reserve symbol map | **Done — phase 118 (2026-05-04).** Added `--symbol-map-json PATH` to `scry solana marginfi-reserves`; the existing `mints: &[MintEntry]` fetcher arg (previously hardcoded to `XSTOCK_MINTS`) is now merged with an optional caller-supplied `Vec<{symbol,mint}>` JSON file (xStocks always present; JSON wins on mint collision). New `ops/sources/data/marginfi-symbol-map.json` ships 10 mappings (USDC + SOL from `scryer-fetch-solana::types::mints`; 8 xStocks from `scryer-fetch-dexagg::jupiter::XSTOCK_MINTS`). Re-snapshot `--all --symbol-map-json …` wrote a fresh `day=04.parquet` (422 rows, 1 partition); symbol coverage went from 0/422 non-`?` to **110/422 non-`?` (99 USDC + 11 SOL banks)**. Remaining 312 banks still `?` — their mints aren't in the 10-entry map; expanding the map is a future operator job. Per the methodology lock "Live reserve validation found no direct xStock Banks", the 8 xStock entries are inert against the current bank set but kept for parity. Bank-registry verification: `scry solana marginfi-liquidations --start 2026-12-01 --end 2026-12-01` reloads `banks_loaded=422` from the new partition. Existing 677 liquidation rows on `[2026-05-01, 2026-05-03]` retain `("?","?")` per_pair until the operator deletes and re-fetches those partitions (same carry-forward as 47.1/47.1.b). No schema change; 1009 lib tests still pass. |
| 47.1.b cross-IX withdraw walk | Walk same-tx `lending_account_withdraw` IXs and attribute their inner SPL Token Transfers (out of `bank_liquidity_vault`, into the liquidator's withdraw target ATA) back to the matching `lending_account_liquidate` IX. | 47.1 captures only the insurance-fee fragment within the liquidate IX itself. For the dominant flashloan-arb pattern the gross seizure (e.g., 32,207,412 in the spec sample) lives in the following withdraw IX and is currently absent from `asset_amount_seized`. |

### Original spec (preserved for context)

- Methodology: `MarginFi-v2 schemas - 2026-04-29` (amended 2026-05-03 + 2026-05-04 phases).
- Schema reference: `docs/schemas.md#marginfi_liquidationv1`.
- Goal: per-liquidation MarginFi-v2 event panel with oracle context and fee split fields.
- Operational reason: Kamino xStock liquidation scan returned sparse/zero events; MarginFi is the active event source for Paper-3 (validated: 677 liquidations in 3 days).
- Join requirement: keep `signature` / `fee_payer` suitable for `jito_bundles.v1` OEV joins. (`fee_payer` populates correctly.)

## Item 51 - Paper-4 Phase-A Capture

Methodology: `Paper-4 Phase-A capture spec - slot-resolution xStock AMM panel - 2026-05-01` and `jito_bundle_tape.v1` amendment.

| Sub-Item | Artifact | Status | Next Action |
|---|---|---|---|
| 51a | `jito_bundle_tape.v1` forward-poll daemon | done — phase 97 | None. Per-manifest plist `runner-jito-bundle-tape` is firing on cadence; partial-coverage caveat documented. Future: long-lived KeepAlive daemon with internal pacing for full coverage (v2). |
| 51b | `validator_client.v1` per-epoch refresh | done — phase 98 | None. Manifest runs hourly on multi-manifest `runner-tick`; BAM detection via Jito kobe API integrated. Future refinement: broader gossip view to lower the ~60% unknown rate (dominated by Stakewiz validators absent from the proxy provider's `getClusterNodes` view). |
| 51c | `clmm_pool_state.v1` forward-capture | done — phase 103 | None. `scryer-fetch-solana::clmm_pool_state` ships with hand-coded Whirlpool + Raydium-CLMM decoders; manifest runs on `runner-tick` at 60s. Curated 40-pool list at `ops/sources/data/clmm-pools.txt`; regenerate periodically. |
| 51d | `dlmm_pool_state.v1` forward-capture | done — phase 107 | None. 14 Meteora xStock pools live in `ops/sources/data/dlmm-pools.txt`; runner-tick fires every 60s and writes 14 rows per fire under `_source=rpc:getMultipleAccounts:dlmm-pool-state:runner` against `dataset/solana_dex/dlmm_pool_state/v1/`. Pool list rotates monthly at most. |
| 51e | `dex_xstock_swaps.v1` range tightening + plist | done — phases 99, 101 | Forward-poll manifest live on dedicated Phase-B plist. Still pending: range backfill of `[2025-07-14, forward-cursor)` is a one-shot operator job (use existing `--start`/`--end` mode); intentionally not on the runner. |

Operational order: ✅ bundle tape ✅ validator labels ✅ CLMM state ✅ DLMM state ✅ swap mode/manifest. Item 51 fully shipped. Outside this item: 51e range backfill of `[2025-07-14, forward-cursor)` is a separate operator job, deliberately not on the runner.

## Item 49 - Oracle Coverage-Inversion Historical Panel

| Sub-Item | Artifact | Status | Next Action |
|---|---|---|---|
| 49a | Pyth Hermes Benchmarks >=90d | code shipped phase 71 | Run operator backfill at locked rate; verify canonical partitions. |
| 49b | Kamino Scope >=90d | methodology needed | Lock endpoint/backfill method, then implement/run. |
| 49c | RedStone permaweb >=90d | methodology needed | Lock source and coverage shape, then implement/run. |
| 49d | Chainlink Data Streams >=90d | operator-run | Run historical coverage and cut over soothsayer consumer. |

Critical carry-forward: Pyth Benchmarks uses the range endpoint and locked 4 req/s sustained ceiling.

## Cross-Repo Soothsayer Items

| Item | Status | Note |
|---|---|---|
| 42 relay scaffold + Verifier-CPI integration | in flight | Program-side work; coordinate outside scryer. |
| 43 `chainlink_streams_relay_tape.v1` | in flight | Depends on relay scaffold; mirror tape lives in scryer once source exists. |
| 54 `oracle.soothsayer_v6.band_tape.v2` mirror | **paused 2026-05-03** — soothsayer-side methodology under revisit | Scryer-side schema/fetcher/CLI are in tree (phase 110); manifest is parked at `ops/sources/soothsayer-band-tape.toml.paused`. Resume after the new soothsayer methodology lands; if wire format changes, the scryer-side schema is at risk of a v3 bump. Do not coordinate further with the soothsayer publisher work until the methodology is settled. |

## Blocked / Gated

| Item | Status | Unblocker |
|---|---|---|
| 45 Phemex OHLCV | blocked | Public kline endpoint requires non-US/VPN/auth path; ticker endpoint works. |
| 36 `dex_treasury_swaps.v1` | gated | Multi-class scope decision. |
| 38 `treasury_auction.v1` | gated | Multi-class scope decision. |
| 54 `oracle.soothsayer_v6.band_tape.v2` | paused — methodology-dependent | Soothsayer-side methodology under revisit; publisher daemon likely to be redone. Resume only after the new methodology lands and locks the wire format. Scryer-side code is shipped and dormant (manifest at `ops/sources/soothsayer-band-tape.toml.paused`). |
| Soothsayer B1 — dex_xstock/swaps backfill widened to 2025-06-30 | gated | Same paid-Solana-RPC blocker as `PR.7` Data-Pending row (Helius/QuickNode/Alchemy at monthly cap, RPCFast key revoked, Foundation-only @ 2 rps). Also gated on a comparator-window methodology update — current lock is `[2025-11-01, 2026-05-01)`; widening to 2025-06-30 needs a new dated entry under `xStock comparator window` in `methodology_log.md`. Resume order: (1) paid provider returns to `weight=1`, (2) decide on widened-window methodology lock, (3) un-pause `dex-xstock-swaps.toml.paused`, (4) re-launch the operator backfill. See soothsayer prompt 2026-05-10 §3.B1. |
| Soothsayer B2 historical — cex_stock_perp/ohlcv multi-venue REST backfill | gated | Forward-direction is deliverable today via consumer-side resample of multi-venue `cex_stock_perp/tape/v1` (already 11-venue). Historical backfill before tape stand-up needs per-venue REST OHLCV fetchers (gate, htx, bingx, phemex, okx, kucoin_futures, bitget, mexc, coinbase_intl, crypto_com) — each is a fetcher-crate addition with its own rate-limit / pagination shape, ≈ 8-10 weeks of fetcher work. Bonus pre-flight: per-venue xStock-perp listing-date table (currently no authoritative source; first-non-empty-bar inference is the fallback). Authorize sequencing before starting. See soothsayer prompt 2026-05-10 §3.B2. |
| Soothsayer B3 historical — cex_stock_perp/tape backfill to 2025-12 | blocked | Most CEX tape products do not expose historical L1-quote tape via free REST; tape is a real-time WS surface only. Closing soothsayer's 2025-12 backfill ask requires either (a) a paid tick-data vendor like Tardis per venue, or (b) accepting that the OHLCV-from-tape resample is the panel for the historical slice. No free-tier path identified. See soothsayer prompt 2026-05-10 §3.B3. |
| Soothsayer B4 — geckoterminal multi-pool xStock registry | gated on small CLI extension | `bin/scry/src/dexagg_cmd.rs` `GtTradesArgs::pool: String` is single-valued today. Plan: extend to `Vec<String>` (or new `--pools-file <path>`) with rate-limited per-pool loop (default `--rate-limit-ms 2000` to stay well under GeckoTerminal's free-tier 30-req/min ceiling), then add `ops/sources/data/geckoterminal-pools.txt` seeded from `clmm-pools.txt` + `dlmm-pools.txt` xStock pool addresses, plus a sibling `ops/sources/geckoterminal-xstock.toml` manifest. ~1 day of fetcher + manifest work. No $ cost. Deferred from 2026-05-10 turn to keep that turn's scope tight. See soothsayer prompt 2026-05-10 §3.B4. |
| Soothsayer F4 — competitor_oracle_analytics_rollup.v1 | gated on methodology lock + implementation | New rollup schema joining `pyth/oracle_tape/v1` + `chainlink/data_streams/v1` + `redstone/oracle_tape/v1` + `kamino_scope/oracle_tape/v1` per soothsayer's `reports/paper4_oracle_conditioned_amm/scryer_data_asks.md`. Needs methodology entry (row schema, cadence, dedup key) before implementation per Hard Rule #1. Implementation shape is a new `analytics-*` manifest (sibling to `analytics-freshness-check`/`analytics-workflow-runs`/`analytics-dead-letter`) running `scry analytics competitor-oracle-rollup` on hourly + daily cadences. No $ cost. See soothsayer prompt 2026-05-10 §1.F4. |
| Soothsayer N1 — free off-hours executable equity reference | **partially closed 2026-05-10** | **Blue Ocean ATS overnight slice (8 PM – 4 AM ET Sun-Thu) IS available free.** Probe 2026-05-10 00:30Z (Sun 8:30 PM ET) against the new Pyth Lazer fetcher confirmed all 10 equity feeds (`Equity.US.{SPY,QQQ,AAPL,GOOGL,NVDA,TSLA,MSTR,HOOD,GLD,TLT}/USD`) return live prices with realistic spreads on the free Pyth Terminal API key — Blue Ocean's executable overnight pricing flows into the public feed-id space, not paid-Pro-tier-gated as soothsayer's docs assumed. SPY=$736.5, AAPL=$291.4, NVDA=$214.7, etc., with 3-4 publishers per equity feed and ~150-byte signed Solana payloads on every row. **Saturday + Sunday-pre-Blue-Ocean (the canonical xStock weekend gap) remains uncovered** — Lazer publishes stale-hold prices then; only paid sources (Polygon, Tradier, Databento US-equity 1m) cover it. Item promotes from blocked to "weekend-only-blocked" — re-propose only with paid-source budget for the Saturday + early-Sunday slice. See `Pyth Lazer fetcher` row above. |
| Soothsayer N2 per-block — slot-resolution xStock pool-state via geyser | gated | `solana_dex/{clmm,dlmm}_pool_state/v1` is shipped (51c/51d) at **60s sampling** via `getMultipleAccounts` on `runner-tick`, not slot-resolution. True per-block / per-slot capture needs a geyser subscription (Helius/Triton/QuickNode), which is the same provider-budget question gating B1. Resume after paid provider returns + geyser-provider choice methodology lock. See soothsayer prompt 2026-05-10 §5.N2. |

## Item 53 - `nasdaq_halts_intraday.v1`

- Status: code-shipped + runner-live 2026-05-03 (phase 109). Data-pending until at least one halt event lands within Yahoo's 7d horizon and gets backfilled by the daily fire — the past-30d halts in `nasdaq_halts.v1` are all on 2026-04-24, just outside the horizon as of ship.
- Methodology: companion to `nasdaq_halts.v1` per `docs/schemas.md#nasdaq_halts_intradayv1`; FK-keyed via `halt_event_id = Halt::dedup_key()`.
- Soothsayer consumer: W6 oracle-band coverage during NASDAQ halts (Paper-3 §Structural complement). `VALIDATION_BACKLOG.md` W6 in the soothsayer repo.
- Source: Yahoo Finance public `/v8/finance/chart` at `interval=1m` (no auth, browser UA only). 7-day rolling backfill horizon — older halts cannot be captured from this source. Promote to a paid intraday venue (Polygon, Tradier, Databento US-equity 1m) if the analysis needs deeper history; the row schema is shared and a sibling fetcher landed under `scryer-fetch-equities` would write to the same partition tree.
- Forward path: `ops/sources/nasdaq-halts-intraday.toml` runs `scry nasdaq halts-intraday --lookback-days 7` daily at 22:30 UTC under the existing runner-tick plist; FK column lets the same minute-bar be tagged with multiple halt_event_ids when a symbol gets halted multiple times the same day.
- Symbol scope: every halted symbol in the lookback window — not strictly the Soothsayer 10-symbol universe. Rationale: the Soothsayer universe (SPY, QQQ, AAPL, GOOGL, NVDA, TSLA, HOOD, GLD, TLT, MSTR) had **zero** halts in the past 30 days. A strict scope would produce an empty dataset for months; the fetcher writes bars for any halt and lets Soothsayer-side joins filter as needed.
- Soft dependency: `nasdaq_halts.v1` must be reasonably fresh. No runner manifest exists for the halts table itself today (operator-fed via `scry rss nasdaq-halts`). Adding a `nasdaq-halts.toml` manifest is straightforward future work; this item ships without it because halt-table freshness has not historically been a problem.
- Backfill caveat for Soothsayer-side W6 disclosure: bars older than 7 days cannot be retrieved. The shipped row count for past halts will be 0 unless a paid venue is added.

## Item 52 - `volatility.yahoo.single_stock_iv.v2` (and paid backfill follow-on)

- Status: forward poll live as of 2026-05-03 (phase 107); first runner-driven canonical rows landed (8 symbols, dte=12, `_source=yahoo:options:v7:runner` under `dataset/volatility.yahoo/single_stock_iv/v2/`). Still data-pending per the wishlist's stricter promotion criterion: Done flips after either (a) ~20 forward weekends accumulate, or (b) a paid-venue backfill (OptionMetrics via WRDS or a CBOE archive) lands a sibling `volatility.<paid_venue>.single_stock_iv.v2` manifest with 2014→ coverage.
- Methodology: `Single-Stock IV Schema - 2026-05-02`. Schema reference: `docs/schemas.md#volatilityyahoosingle_stock_ivv2`.
- Operational reason: Paper-1 §7.1 added the F0_VIX baseline rung 2026-05-02. At τ=0.95 with the deployed 0.020 buffer, F0_VIX undercovers by 7pp on the OOS 2023+ slice (realised 0.876, Kupiec rejects p<0.001). The miscalibration is structural — index VIX cannot price per-name weekends — and the F0_singleIV candidate baseline is per-symbol IV.
- Forward path: `ops/sources/yahoo-single-stock-iv.toml` runs `scry equity-options iv-snapshot --source yahoo` daily under the existing runner-tick plist; one row per symbol per day (8 symbols default, GLD/TLT operator-side).
- Backfill path (gated, separate work): a paid venue is still required for the §7.1 head-to-head against F0_VIX (2014→). When access is acquired, add a sibling fetcher module (e.g. `tradier.rs`, `optionmetrics.rs`) under `scryer-fetch-equity-options`, register a new `volatility.<venue>.single_stock_iv.v2` schema id, and ship the corresponding manifest. The row shape is shared.
- Soothsayer consumers (post-data): F0_singleIV ablation rung in the Paper-1 ladder; a per-symbol-IV variant of F1's volatility regressor; the v2 paper's revisit of "what does the right per-symbol implied baseline do?"

## Item 54 — `oracle.soothsayer_v6.band_tape.v2`

- **Status: paused 2026-05-03 — methodology-dependent.** Soothsayer-side methodology is being revisited; the publisher daemon may be redone. Do not invest further on this item until the new methodology lands and the wire format is settled.
- Pause shape: scryer-side code remains in tree at phase 110 (schema `oracle.soothsayer_v6.band_tape.v2`, fetcher `scryer-fetch-solana::soothsayer_band_tape`, CLI `scry solana soothsayer-band-tape`, methodology row, schema doc, `KNOWN_V2_SCHEMAS` registry entry, `ORACLE_SOOTHSAYER_V6` venue constant, `DatasetSchema` impl). The runner manifest is parked at `ops/sources/soothsayer-band-tape.toml.paused` (the `.toml.paused` extension excludes it from `*.toml` glob discovery in `scryer-runner` and `scryer deploy`). The staged copy at `~/Library/Application Support/scryer/manifests/` was removed so `runner-tick` is no longer firing it.
- Resume path: rename `.toml.paused` back to `.toml`, run `scryer deploy`. If the post-A4 wire format is preserved by the new methodology, the existing v2 schema works unchanged. If profile/regime/wire semantics change, the resume bump is `oracle.soothsayer_v6.band_tape.v3` (or a new venue) — never an in-place v2 edit, per Hard Rule 2.
- Original methodology lock (still in `methodology_log.md` "Soothsayer Lending-track Band Tape — 2026-05-03"): single venue across Lending + AMM, partition key `profile=lending|amm`, decode delegated to the `soothsayer-consumer` path-dep, dedup `(symbol, publish_slot)`. Keep these for reference; they may need to be amended or replaced when methodology resumes.
- Original goal (still relevant): a parquet receipt history of what soothsayer's Lending oracle actually published on-chain, separate from soothsayer's *predicted* artefact bytes. Two downstream uses: (1) backtests against served-not-predicted bands; (2) profile-code provenance for replays across the Lending/AMM transition.

## Retracted

| Item | Reason |
|---|---|
| 21 `chainlink_streams_tape.v1` | Replaced by `chainlink_data_streams.v1` verifier tape. |
| 22 `switchboard_ondemand_tape.v1` | Not a near-term comparator source. |
| 39 | Premise removed; do not re-propose without new evidence. |

## Recently Shipped Pointers

Use `docs/phase_log.md` for details. Keep this list short and remove entries once they stop influencing forward work.

| Item | Phase | Carry-Forward Note |
|---|---|---|
| 46 `marginfi_reserve.v1` | 69 | No direct xStock Banks; consumers need Kamino-position hop. |
| 48 Chainlink v11 decode | 67 | Filter `market_status` by schema/report version before interpreting. |
| 50 loud-failure ops | 70, 72, 73 | Freshness watchdog, proxy self-clear, and `scryer deploy` are current ops tools. |
| 51 Paper-4 Phase-A xStock AMM panel | 97, 98, 101, 103, 107 | All five sub-items shipped (jito-bundle-tape, validator-client, dex-xstock-swaps forward-poll, clmm/dlmm pool-state). 51e historical range backfill remains a separate operator job. |
| 52 yahoo single_stock_iv first runner rows | 107 | Forward poll live; promotion to Done still gated on ~20 weekends or paid-venue backfill. |
| earnings.v2 session timing | 126 | Two upstreams: Finnhub `hour` (forward only — free tier has no history) + one-shot Yahoo `earnings-backfill` (deep history, ET-derived bmo/amc). v1 frozen; `earnings-migrate` is the cutover. Re-run backfill to extend/correct; runner handles forward. Consumer (soothsayer panel.py single-gap mapping) reads `session` from `yahoo/earnings/v2/`. **⚠️ 2026-07-25 (soothsayer): backfill is stale and it is causing a live consumer defect.** Confirmed sessions for the soothsayer panel symbols fall off a cliff — 2023: 24/24 confirmed, 2024: 24/24, 2025: 12/18, 2026: 5/19. The Finnhub forward runner keeps *dates* current but only the Yahoo backfill confirms *sessions*, and soothsayer's panel excludes `session_confirmed == False`. Net effect: soothsayer's overnight panel has **zero** `earnings_night` rows after 2025-05-28, so any earnings night served today is classified `normal` and gets a ~3% band where the calibrated earnings band is ~25% (measured miscoverage without the cell: 0.333 against a claimed 0.95). ~~Action: re-run `scry equities earnings-backfill`~~ — **disproven 2026-07-25, the backfill cannot fix this.** Ran it against live: `rows_added=0 rows_deduped=461 symbols_failed=0`. Ran it again into a clean dataset root to see its true output: 461 rows, and **Yahoo's earnings history terminates Apr–Jun 2025 for all six symbols** (AAPL 2025-05-01, MSTR 05-22, GOOGL 06-06, HOOD/NVDA 06-26, TSLA 04-22). Not a `--size` cap — GOOGL/HOOD/TSLA return 84/18/59 rows against a cap of 100. The upstream is *dead, not stale*; the backfill exits 0 and reports success on every future run. Two further latches, independent of the source: (a) `merge_dedup` is **first-writer-wins** (`crates/scryer-store/src/lib.rs:397`), and `earnings-migrate` was run 2026-04-27 — *before* the 2026-05-25 backfill, inverting the ordering `equities_cmd.rs:226` documents as mandatory — so 20 legacy `session=unknown, session_confirmed=NULL` rows permanently shadow the 2025-07 → 2026-04 quarters; even a live Yahoo would return `rows_added=0`. (b) Because identity is `(symbol, earnings_date)`, a Finnhub *date revision* writes a second row rather than updating the first — 8 duplicate-quarter clusters exist (AAPL 07-29/07-30, GOOGL 07-21/07-22/07-28, MSTR 07-29/07-30/08-04, …), which manufactures *phantom* earnings nights, the opposite error. **Proposed fix: bridge `edgar_8k.v1` → `earnings.v2` session timing.** Verified 2026-07-25: `scry sec edgar8k` over the six symbols returns 604 filings, and item-2.02 acceptance times in ET close **19 of the 22** dark rows in the panel window exactly (AAPL/GOOGL/NVDA all `amc` ~16:01–16:31; TSLA 2026-04-22 `amc` 16:10). Join **on known earnings dates only** — a naive "2.02 = earnings" rule adds 8 phantoms (TSLA's quarterly delivery 8-Ks at ~09:05 ET, MSTR preliminary 8-Ks, HOOD extras). The 3 residual dark rows (GOOGL 2025-06-06, HOOD/NVDA 2025-06-26) have no 2.02 filing and are spurious Yahoo placeholder dates — correctly excluded today, and candidates for deletion. Fails silently in the safe-looking direction — a missing earnings night is a `normal` row, not an error — and so does the repair action, which is worse. |
| yahoo_corp_actions overnight backfill | 127 | Ex-dividend source for soothsayer's overnight gap adjustment. Code pre-existed (phase 61); landed the 10-name backfill 2026-05-25 over full Yahoo history (SPY→1993). Backfill-only, no runner (corp-action history is static — re-run `scry equities corp-actions` to extend). Consumer mapping: ex_date=`event_date`, cash_amount=`dividend_amount`, action_type=`event_type`. GLD/HOOD legitimately empty. |
| 54 `oracle.soothsayer_v6.band_tape.v2` | 110 | Code-shipped, runner-live; one venue across Lending+AMM with `profile=lending|amm` partition key. Decode via `soothsayer-consumer` path-dep. Awaiting cross-repo publisher daemon for first row. |
| LVR Job 2 | 79 | Helius enhanced path supersedes Flipside; 26h spot-check gates 180d run. |

## Methodology Entries Needed

- Kamino Scope >=90d historical backfill source/method.
- RedStone permaweb >=90d historical backfill source/method.
- Any new v2 schema migration that changes vocabulary or row semantics.

## Suggested Execution Order

1. Implement item 47 if Paper-3 event measurement is the immediate blocker.
2. Otherwise start v0.2 prerequisites: manifest format + `SchemaId` enforcement.
3. For Paper-4, ship item 51 in sub-item order.
4. For Paper-1 oracle coverage, finish 49a operator run before adding 49b/49c complexity.
